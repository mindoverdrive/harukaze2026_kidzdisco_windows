# ブロック演出の追加候補

基準は `69c628a`（粒子トランジションの目視合格は `c804013`）。

- 5配色：prism / aurora / ember / acid / lagoon。
- 解除は落下・上昇・左右へ開く・中心から外へ散る4種。配色と組み合わせて切り替える。
- 80回の周期のうち60回はブロック単位のランダム配色、20回は色帯。乱数は切替番号を種にし、同じ切替中に色を引き直さない。
- 大小・縦横比を混ぜた不均等な矩形分割を4組、起動時に作成。全面を隙間も重複もなく覆う。固定数のスプライトとレイアウトを再利用し、フレームごとのSurface生成は増やさない。
- カメラ・ロゴ・文字を含む現在の合成画面をブロックとして動かす。全面遮蔽・次シーンFIRST_FRAME・旧シーン終了・解除の管理経路は変更していない。本番各シーンも変更なし。

## 検証

- Python 3.12: 412テスト成功（skip 1）、3.11: 405成功（skip 40、実描画依存など）。
- 各分割の全画素1回被覆、形の多様性、切替中の固定、色の再現性、透明キー色混入なしを確認。
- 全解除動作で開始画素一致、終端前の全ブロック退場、終端残留なし、キャッシュ再利用を確認。
- 稼働中の旧Managerを認証済みquit経路で終了し、旧プロセス不在を確認して新版を起動。ログは `test_reports/navigation_variants_20260907.stdout.log` と同名stderr。
- FractalのREADY/START_ACK/FIRST_FRAMEとpromotionを確認。これは表示開始の機械的確認であり、人間の見え方の合格判定ではない。
- 実行記録 `kids_trial_20260907_182134_759127400` の scene_output でcycle 1〜8の新配色・4動作の選択を確認。旧シーン停止要求に続くKeyboardInterrupt出力あり。演出のdisabled通知はこの範囲ではなし。例外ゼロとは記録しない。

## Human Check Required

XiaomiでNextを約1秒保持し、切替完了後に手を離して再度行う。4回を目安に、ブロックの配色・形・動き、ロゴと文字の読みやすさ、映像のつながりを確認する。OKは色や形が途中でちらつかず、解除後に残留せず、デスクトップ/タスクバーが出ないこと。明るさや速さの好みは人間判定。

新演出版の長時間・子供・複数人確認は未実施。この記録後にRe:birth原本の交互表示と白抜きを追加し、白抜き表示はユーザー目視OK。全解除パターンの組合せを合格とするものではない。今回の身体シーン/Mandala更新と合わせて候補チェックポイントに保存する。最新結果は `BODY_SCENES_AND_MANDALA_20260907.md` を参照。戻す場合は演出プロセスを正常終了後、候補コミットのrevertまたは基準の別worktreeを使い、無関係な作業ファイルを一括で戻さない。
