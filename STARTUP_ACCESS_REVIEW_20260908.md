# 起動時アクセス拒否の短時間切り分け

基準: 92ef91c。Manager・Transition・シーン・Next/Back・操作仕様は変更していない。

## 確認した事実

- operator_20260908_000022/runtime.jsonl: 起動6.672秒でfailed launch setupの停止要求、6.687秒でWinError 5、7.172秒でexit_code 1。シーン昇格0回。
- manager.pyの_spawn_processではPopen成功後、WindowsSceneJobの準備または診断出力設定が失敗したとき、この停止記録を出す。プロセス作成そのものの失敗ではない。
- WindowsSceneJobにはJob作成・設定・登録・既に生成された子プロセスの取得/登録がある。元ログにはこの内部の例外行がないため、どのAPIが拒否されたかは断定できない。
- 設定・権限変更なしの直後再試行ではFIRST_FRAME 2.703秒で起動成功。
- 今回、同じ仮想環境・CREATE_NEW_PROCESS_GROUP・WindowsSceneJobで20回の短時間登録試験を実行。失敗0、全プロセス終了、全Job空を確認。カメラや観客画面は操作していない。
- 証拠: test_reports/probe_job_start_20260908.py と .json。これはプロセス所有の下位経路試験であり、Manager全起動20回やGPU初期化試験ではない。

## 判断と区切り

常時の権限不足は再現しない。競合や短命な子プロセスなど一時条件の可能性はあるが、原因確定ではない。初回起動を阻害し得るため本番影響ゼロとはしない。既存の終了経路は失敗を検知し、成功扱いせず起動を中断している。

短時間調査はここで区切る。推測によるManager変更、管理者権限での迂回、Job管理の解除はしない。再発時は完全終了を確認後に同じ起動方法で再試行し、繰り返す場合は追加シーンの本番投入を止め、例外の内部API行を取得する。

## 追加シーン投入への準備

- 追加シーンの調査・隔離試験は進められる。本番起動の恒久対策完了とは区別する。
- 運営用12シーンの保存プロファイルは維持。新候補を自動追加しない。
- 次候補は既存優先表の未採用Earthを最初に再読取し、無人演出としての差とWGPU/素材依存を確認する。旧優先表の既採用Wave/Tree/Skeletonは再移植しない。
- 候補単独の共有カメラ・FIRST_FRAME・終了/再起動を検証し、目視後に一時プレイリストで既存Transition経由の往復を確認する。合格後だけ運営一覧へ追加する。
- 子供・複数人・12時間試験は独立した未確認項目。

今回の成果は調査記録のみ。製品コードの変更なし。
