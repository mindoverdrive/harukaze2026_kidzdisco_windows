# MVPの次工程：合格基準点の固定と最小リスクの対応案

2026-09-07。今回は基準点の固定、資料・コード・直近ログの照合、次の1項目の対応案作成まで。製品の実装変更、新たな実機試験は行っていない。

## 固定した基準点

- ユーザーの音声入力「QCAA六二AO」は、直前に報告した `9caa62a` と照合して確定。
- 完全SHA：`9caa62ae1832c5bb600476ebff8567d40354083f`。
- 注釈付きGitタグ：`codex/storm-visual-baseline-20260907`。ローカルとGitHubへ保存し、リモートの参照先が上記SHAであることを確認済み。このタグを移動・上書きしない。
- 合格範囲：Particle Stormの現在の粒径/配置/操作感を基準として維持。成人による中央・上下左右の指位置一致、グー/パーの吸引/反発、継続吸引後の奥行き維持を確認済み。
- この合格をUSB自動復帰、修正後30分/12時間、子供、実2人の合格へ広げない。本番採用やmain/stableへの昇格ではない。以前の安全基準 `705b081` も維持する。
- 今回のPythonとconfigsの差分は基準点から0。挙動は変更していない。以後の記録コミットで候補ブランチのHEADが進んでも、このタグが合格時の状態を指す。

## 直近の試験は終了済み

`test_reports/kids_trial_20260907_075145_394133100` は08:06:56に `duration_reached` / exit0 / `trial_elapsed_s=900.859` / promotion1 / switch0で終了。今回の読取確認で対象のPythonプロセスと共有メモリは不在。これは15分試験であり、過去の別起動と合算して30分連続の合格にしない。新しい試験はまだ起動していない。

## MVPの未完了項目

更新の古い「未確認」は後続の各シーン記録・USB原因記録・Stormの最新目視記録で上書きして整理した。希望シーンすべての実装をMVP必須へ広げない。

| 項目 | 現在の根拠・未完了範囲 | 影響範囲・判断 |
| --- | --- | --- |
| **1. 現Storm基準版の30分連続と終了後解放** | 旧版の30分は合格済み。奥行き/粒径/画面比率修正後は短時間目視と15分まで。 | **次に選ぶ。製品コード変更0。既存の試験時間指定とログで実施可能。** |
| 2. C922nのUSB自動復帰 | 抜差しで黒フレームが継続。`read=True`に対し`grab=True/retrieve=False`を同一不良captureで確認。一度の明示再生成では映像・LED復帰を確認したが、自動復帰修正・再試験は未完了。 | 共有カメラの定常取得、起動時制御、FPS診断に関係し全シーンへ影響。重要だが今回の最小リスクの1件には選ばない。 |
| 3. 停止時の例外と通知の整理 | 正常な停止要求の後にも`KeyboardInterrupt`と、閉じた起動用接続へのERROR通知失敗が出る。残留の有無と例外分類は別。 | 共通Runnerの例外・通知・cleanupが対象。原例外や起動途中の失敗を隠す危険があるため、見た目だけのログ抑制はしない。 |
| 4. 本番に使う組合せと切替範囲の確定・検証 | Spheres↔旧Stormは目視往復と自動20回/残留なしを確認済み。最新Stormでの切替再確認、他の採用組合せ、最終の明示プレイリストは別。 | まず採用範囲を絞る。全希望シーン・全組合せの再試験を自動で増やさない。 |
| 5. 現場条件と保存設定の確認 | Spheres約1.5mの成人確認は済み。会場照明・配置・距離、露出/zoomの確定と保存→再起動後の実値確認は残る。現在の比較値-4/176は一時適用、保存JSONは変更していない。 | Human Check Required。暗所の追込みは現場調整という方針を維持。Mac/PANは対象外。 |
| 6. 子供による遊びの成立 | 参加者不在。成人確認で代用しない。 | Human Check Required / 子供参加待ち。参加可否を繰り返し尋ねない。 |
| 7. 実2人の同時操作 | 2人目不在。成人1人の両手は確認済みだが別条件。 | Human Check Required / 2人目待ち。子供確認とも独立に残す。 |
| 8. 最終構成の12時間と資源傾向 | 本番構成での12時間は未実施。GPUメモリがnullの記録はGPUリークなしの証拠にならない。 | 基礎・切替・復帰の後。取得不能な観測値は未確認として記録。 |

Dots、Grid、Spheres、Spiderの過去の成人基本操作・30分合格、Saturnの30分タイマー完走とGLFW修正後目視は保持する。Storm変更だけを理由に全シーンの基本確認をやり直さない。終了例外など各記録に残る制限も保持する。

操作ガイド/妖精、切替時の発光、粒子循環、新たな演出、追加希望シーンの版選定・導入は [MVP後の要望](POST_MVP_INTERACTION_GUIDANCE.md) や将来の選定として扱い、今回選ぶ1項目へ混ぜない。

## 選ぶ1項目：基準版の30分連続確認

### 理由と最小対応

既存 `scripts/start_kids_test.py` は `--duration-minutes` を受け、`manager.py --duration-seconds` へ渡す。`--scene particle-storm --audience` はStormだけの一時profileを作り、共有カメラ・Xiaomi表示を使う。新コード・新設定項目・共通処理の修正が要らないため、USB復帰やRunner修正より影響範囲が小さい。

提案は、従来15分だった試験起動の引数だけを31分にして、同じ製品・同じカメラ条件で正味30分を検証すること。起動直後の比較値適用に最大1分の余裕を取り、適用完了から30分以上を計測する。永久設定は変更しない。実行用の既存コマンドは以下。

```powershell
rtk proxy C:\Users\go\.gemini\antigravity\scratch\harukaze2026_kidzdisco_windows\.venv\Scripts\python.exe -B -X utf8 scripts/start_kids_test.py --audience --scene particle-storm --duration-minutes 31
```

### 実行するときの順序

1. 基準タグとの差分を確認。Storm本体/ラッパー/共通基盤/設定に予期しない差があれば試験を開始せず、その差を特定する。
2. 前試験の終了・対象PID/共有メモリの解放、他のカメラ所有アプリ不在を確認する。既存の上記入口から1シーンだけを起動し、実行Python・commit・profile・run IDを保存する。
3. 過去の比較手順で露出-4/zoom176を一時適用し、読戻しと最初のフレームを記録する。保存JSONは触らない。適用が1分以内に完了したことを確認し、その完了時刻から30分を評価する。適用遅延などで30分に届かなければ、シーン稼働30分とは区別し、不足時間を合格にしない。
4. 自動/手動切替、USB抜差し、映像設定変更を混ぜない。人が30分ずっと手を上げる必要はない。開始/終了付近の生映像と短い手反応だけ確認し、基本操作の全項目は再要求しない。
5. 終了イベントと対象プロセス世代・窓・共有メモリを照合する。稼働中と停止要求後の例外を分けて記録する。

### 合格条件と記録

- 同じsceneとManagerで正味1800秒以上、切替0、起動昇格1。途中の予期しない終了・再起動なし。
- 通常運転でカメラ取得失敗/再生成0、生映像の更新が続き、開始と終了付近で人が反応を確認できる。人間未回答の項目は数値ログだけで合格にしない。
- 暖機後のPrivate Bytes / Working Set / handles / GDI / USER / 描画fps / camera frame age・gapを序盤と終盤で比較。増加傾向や観測不足は要調査として残し、GPU値nullを合格にしない。1回のメモリ解放前増加だけでリークとも断定しない。
- 上限終了は`duration_reached`、exit0。終了後に対象PID・窓・共有メモリの残留がない。既知の停止時例外は別票に残すため「完全無エラー」と表現しない。
- 失敗時はcommit/profile、runtime.jsonl、scene_output.jsonl、stdout/stderr、時刻、PID/作成時刻、camera取得状態、資源値、ユーザーが見た現象を保存する。失敗を理由にその場でStormの挙動を調整しない。

### リスク・戻し方

コード上の挙動変更は0。実機を約30分占有するため、他のカメラ試験とは同時に行わない。試験を中止する場合は操作パネルから通常終了し、資源解放を確認する。恒久設定を触らないため、戻す製品変更はない。基準点へ戻る必要がある場合も未保存変更を壊すreset/cleanは使わず、保存・差分確認後に通常のrevert等で戻す。

## 根拠資料

- [Stormの最新修正・目視記録](PARTICLE_STORM_DEPTH_FIX_20260907.md)
- [MVP目視工程の履歴](MVP_REMAINING_VISUAL_CHECKS_20260907.md)
- [USB原因の実機照合](USB_RECOVERY_CAUSE_ANALYSIS_20260907.md)
- [異種切替20回の結果](MIXED_SCENE_MVP_REVIEW_20260907.md)
- 各シーンの `*_MVP_REVIEW_*.md`。古い総括の「未確認」より、後続の個別結果を優先した。

今回、新しい製品テストを走らせる変更はない。既存の最終回帰230件（描画Python skip1、Python3.11 skip5、失敗0）は基準版の検証履歴として保持する。今回の確認はGit差分/タグ参照・起動引数・15分終了/解放・資料整合性の読取であり、30分の結果ではない。
