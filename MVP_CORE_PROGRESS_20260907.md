# MVP継続記録：Storm中心引力の最小修正

- 固定タグ9caa62aは移動しない。26分07.782秒で中断した旧30分試験は、目視で薄い平面に潰れるためNG。完走・合格に数えない。
- 最小変更：Particle Stormの引き寄せのみ、XY距離180以内で力を0、180〜360で滑らかに増やし、それより外側は元の強さ。Z力0・既存Zノイズ/減衰・パーの反発・カメラ/表示経路は維持。
- 数値確認：3分相当のZ幅と投影後XYの広がり、中心域、外側の従来力、手の有無によるZ運動一致。回帰231件、失敗0、skip1。
- 人間の短時間目視：修正後の引き寄せと広がり・奥行きについてユーザー「ok」。30分の終盤目視とは区別。
- 10分試験 `kids_trial_20260907_090354_75746300` は600.719秒、duration_reached、exit0、promotion1/switch0。前回共有メモリ解放を確認後、次試験を起動。
- 修正後試験 `kids_trial_20260907_091551_126282100` はユーザーの「その30分テストは、俺の判断で合格扱い。次に進め」により受入合格。09:30:55に通常終了、実測894.063秒（14分54.063秒）、operator_quit、exit0、promotion1/switch0、共有メモリ消失確認。これはユーザー判断による受入であり、実測30分完走ではない。露出−4/zoom176を固定、保存JSON変更なし。

## 次の優先順位

1. 修正後Stormはユーザー判断で受入合格。実測30分未完走の制限を保持し、本人の指示により次へ進む。
2. USB自動復帰：修正・単回目視復帰・終了後解放まで確認済み。反復耐久は未確認。暗所の正常黒フレームは失敗扱いしない。
3. 停止時通知：不要な起動用TCPへのERROR送信を修正済み。元例外・cleanupを維持し、実20回切替で不要通知0を確認。停止要求後のKeyboardInterruptは区別して保持。
4. 最新版Spheres/Stormの20回切替反復は完走。本番採用範囲の確定は未回答。採用範囲を無断で全希望シーンに広げない。
5. 最終カメラ設定の保存→再起動で読戻し確認、現場照明/距離条件。
6. 子供の実操作（参加者待ち）。成人確認で代用しない。
7. 実2人同時操作（2人目待ち）。子供確認とは独立。
8. 最終構成の12時間とGPUを含む資源傾向。30分から推測しない。

新演出・妖精ガイド・粒子循環などはMVP後。Mac/PANは対象外。USB原因記録後半の「未実行」は先行時点の履歴で、冒頭と065838後続結果を優先する。

## USB自動復帰の最小修正（単回の実機復帰確認済み）

- shared_camera.pyの物理取得3経路（FPS診断・初回制御確認・定常取得）で、DirectShowはgrab/retrieve両方の成功を必須にした。他backendは従来readを維持。正常な黒い映像は受理する。
- 連続失敗30回または5秒で既存release/reopen経路へ渡す。約1秒の失敗30回待ちを避ける候補値であり、native呼出しや実機30秒復帰を保証しない。
- 成功で失敗継続時間をリセット。既存所有権・共有メモリ・バックオフ・制御値復元経路は維持。
- 修正時点で隔離モック40件成功、本線回帰239件失敗0（skip1）、git diff --check成功。後続の実機単回復帰は以下で確認済み。取得通知だけを復帰成功にしない。
- この段階の製品変更はStorm中心引力とshared_cameraの取得判定のみ。後続のチェックポイント454a7e7へ保存済み。

### 09:45 USB再試験の結果

- 先行093203試験は600.828秒で自動終了。フレーム再開は記録されたが、目視回答が曖昧だったため目視合格には数えなかった。
- 再試験 `kids_trial_20260907_094331_844726200`：利用者が「映像・反応ともOK」と正常状態を確認後、5秒抜いて同じポートへ戻す手順を提示。「戻した」の報告後、ランプ・生映像・手反応がすべて復帰したことを「はい」で確認。
- 実測ログ：一時C922消失→自動再取得、最大フレーム間隔17.875秒、read_failures累計6、reopen_attempts5。これは再取得試行数であり、成功5回ではない。抜差し時刻を独立計測していないため、USB挿入からの厳密な復帰秒数とは区別。
- 復帰後sample136.875秒でframe_id1817/age0/last_errorなし。Manager15264・scene26528・wrapper8380、共有メモリ名を維持。promotion1、切替0、シーン再起動なし。
- 判定：この単回のUSB自動復帰と人間の目視は合格。反復耐久・子供・実2人・12時間は別。今回起動は後述のとおり終了済み。
- 後続確認：09:55:07にoperator_quit、exit0、685.343秒で通常終了。対象PID 23800/15264/8380/26528は不在、共有メモリ消失を確認。今回の終了後解放は確認済みに更新。
- チェックポイント：上記Storm/USB変更を候補ブランチ `454a7e7` にコミット・push済み。main/stableおよび固定タグを変更していない。

## 停止時通知と最新異種切替

- RunnerでFIRST_FRAME送信成功後の起動用TCPへのERROR通知だけを省略。元の例外・KeyboardInterrupt・cleanup・起動途中のERROR通知は維持。終了時のKeyboardInterrupt自体を無条件に隠してはいない。
- 回帰243件、失敗0（skip1）。修正後の実機Spheres/Storm20回切替 `mixed_repeat20_20260907_095703_206815/trial` は次項のとおり完了。20秒間隔・20回上限・1800秒安全上限、既存2シーン試験profileを使用。本番の採用リストはユーザー回答待ち。
- 後続結果：10:05:04、switch_count_reached / exit0 /472.594秒で20回完走、21回FIRST_FRAME。20回すべて新しいFIRST_FRAME後に旧シーンを停止。記録43プロセス世代と共有メモリの残留なし、定常3プロセス、カメラ失敗/再生成0、最大frame gap0.079秒。不要なERROR通知失敗0。例外を含む53出力レコードはすべて該当停止要求後で、稼働中例外は0。KeyboardInterrupt自体は隠していない。
- この自動結果を20回すべての人間目視、GPUメモリ、子供、実2人、12時間へ拡張しない。USB後の定常復帰と最新2シーン切替の基礎は確認済み。本番採用リストの回答後に恒久入口・保存設定・長時間構成を確定する。
- 停止通知と切替記録は候補ブランチ `44ff0ed` にcommit/push済み。

## 入口と最終ゲート

- このフォルダーの.venvが不在だったため、Start Rebirth Acer.cmdに存在確認付きで従来の検証済みPythonを選ぶ補完を追加。明示KIDZDISCO_PYTHONとローカル.venvが優先。環境のインストール・更新はなし。
- 実cmd経由の `--check --scene particle-storm` がPython3.12.10、失敗0、DISPLAY1/5の1920x1080拡張配置で成功（kids_preflight_20260907_100657.json）。このcheckはカメラ・GPU起動をしない。
- GPUProcessMemoryのWindows性能カウンターは読取可能と確認。ただし既存runtimeのgpu_bytesはnullのままで、過去の試験にGPU合格を追加しない。
- 本番リストの選択をユーザーに質問済み。未回答で2シーンを最終採用と断定しない。子供・実2人・会場条件・12時間は未完了のまま。全体MVP完了宣言はしていない。
- 独立GPU観測器を追加。`kids_trial_20260907_100932_153369400`のStormで2回取得し、専用323047424 bytes・共有202334208 bytes。PID世代前後照合、重複インスタンス排除、取得不能nullのテスト4件成功。71.406秒で通常終了、共有メモリ消失、観測器のrun_end検出を確認。短い観測でありGPU耐久合格ではない。
- 観測器追加後の回帰247件、失敗0（skip1）。対象PID23936/13000/18796/21528は不在、git diff --check成功。描画本体・定常ログの収集処理は変更していない。

## 完了監査：MVP全体は未完了

最新製品チェックポイント64970bcとその試験記録を再読取。候補ブランチはremoteと同期、未保存の製品変更なし、稼働Pythonなし。別作業の未追跡3項目には触れていない。

完了根拠：Storm目視とユーザー受入、USB単回自動復帰、最新20回切替・終了解放、247回帰、実cmdの事前チェック、GPU観測器の短時間実測。これらの結果を本番全体の合格には拡張しない。

残る依存関係：本番採用リストの選択→そのリストの恒久起動設定→カメラ設定保存と再起動の実値確認→最終構成で12時間。シーン選択の回答なしに2シーン構成を本番確定しない。子供・実2人・会場照明/距離の実地確認は別々に人間/参加者が必要。

本番リスト未回答は3回の連続したgoal継続でも解消していない。回答を待つ間に停止通知、20回反復、起動環境補完、GPU観測準備を完了した。次の本番確定は人間の選択に依存するため、完了ではなく入力待ちとして引き継ぐ。参照された旧「未実装/継続中/結果待ち」表示を上記の最新結果へ整合した。
