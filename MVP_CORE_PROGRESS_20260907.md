# MVP継続記録：Storm中心引力の最小修正

- 固定タグ9caa62aは移動しない。26分07.782秒で中断した旧30分試験は、目視で薄い平面に潰れるためNG。完走・合格に数えない。
- 最小変更：Particle Stormの引き寄せのみ、XY距離180以内で力を0、180〜360で滑らかに増やし、それより外側は元の強さ。Z力0・既存Zノイズ/減衰・パーの反発・カメラ/表示経路は維持。
- 数値確認：3分相当のZ幅と投影後XYの広がり、中心域、外側の従来力、手の有無によるZ運動一致。回帰231件、失敗0、skip1。
- 人間の短時間目視：修正後の引き寄せと広がり・奥行きについてユーザー「ok」。30分の終盤目視とは区別。
- 10分試験 `kids_trial_20260907_090354_75746300` は600.719秒、duration_reached、exit0、promotion1/switch0。前回共有メモリ解放を確認後、次試験を起動。
- 修正後試験 `kids_trial_20260907_091551_126282100` はユーザーの「その30分テストは、俺の判断で合格扱い。次に進め」により受入合格。09:30:55に通常終了、実測894.063秒（14分54.063秒）、operator_quit、exit0、promotion1/switch0、共有メモリ消失確認。これはユーザー判断による受入であり、実測30分完走ではない。露出−4/zoom176を固定、保存JSON変更なし。

## 次の優先順位

1. 修正後Stormはユーザー判断で受入合格。実測30分未完走の制限を保持し、本人の指示により次へ進む。
2. USB自動復帰：readの偽成功を、物理captureのgrab/retrieve両成否確認で防ぐ最小変更。065838診断で同一captureのread=True、grab=True/retrieve=Falseを2組確認し、1回の明示再生成後の映像/LED復帰も確認済み。自動復帰は未実装。暗所の正常黒フレームは失敗扱いしない。
3. 停止時のKeyboardInterruptと閉じた起動接続へのERROR通知失敗の分類。例外抑制だけで合格にせず、元例外・cleanup維持を確認。
4. 最新版で本番採用範囲の切替反復。採用範囲を無断で全希望シーンに広げない。
5. 最終カメラ設定の保存→再起動で読戻し確認、現場照明/距離条件。
6. 子供の実操作（参加者待ち）。成人確認で代用しない。
7. 実2人同時操作（2人目待ち）。子供確認とは独立。
8. 最終構成の12時間とGPUを含む資源傾向。30分から推測しない。

新演出・妖精ガイド・粒子循環などはMVP後。Mac/PANは対象外。USB原因記録後半の「未実行」は先行時点の履歴で、冒頭と065838後続結果を優先する。

## USB自動復帰の最小修正（単回の実機復帰確認済み）

- shared_camera.pyの物理取得3経路（FPS診断・初回制御確認・定常取得）で、DirectShowはgrab/retrieve両方の成功を必須にした。他backendは従来readを維持。正常な黒い映像は受理する。
- 連続失敗30回または5秒で既存release/reopen経路へ渡す。約1秒の失敗30回待ちを避ける候補値であり、native呼出しや実機30秒復帰を保証しない。
- 成功で失敗継続時間をリセット。既存所有権・共有メモリ・バックオフ・制御値復元経路は維持。
- 隔離モック40件成功、本線回帰239件失敗0（skip1）、git diff --check成功。実機USB自動復帰は未確認。取得通知だけを復帰成功にしない。
- 製品変更はStorm中心引力とshared_cameraの取得判定のみ。コミット/プッシュはまだ保留。

### 09:45 USB再試験の結果

- 先行093203試験は600.828秒で自動終了。フレーム再開は記録されたが、目視回答が曖昧だったため目視合格には数えなかった。
- 再試験 `kids_trial_20260907_094331_844726200`：利用者が「映像・反応ともOK」と正常状態を確認後、5秒抜いて同じポートへ戻す手順を提示。「戻した」の報告後、ランプ・生映像・手反応がすべて復帰したことを「はい」で確認。
- 実測ログ：一時C922消失→自動再取得、最大フレーム間隔17.875秒、read_failures累計6、reopen_attempts5。これは再取得試行数であり、成功5回ではない。抜差し時刻を独立計測していないため、USB挿入からの厳密な復帰秒数とは区別。
- 復帰後sample136.875秒でframe_id1817/age0/last_errorなし。Manager15264・scene26528・wrapper8380、共有メモリ名を維持。promotion1、切替0、シーン再起動なし。
- 判定：この単回のUSB自動復帰と人間の目視は合格。反復耐久・今回起動の終了後解放・子供・実2人・12時間は別。表示は継続中。
- 後続確認：09:55:07にoperator_quit、exit0、685.343秒で通常終了。対象PID 23800/15264/8380/26528は不在、共有メモリ消失を確認。今回の終了後解放は確認済みに更新。
