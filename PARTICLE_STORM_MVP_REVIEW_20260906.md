# Particle Storm MVP確認と共通経路監査

## 2026-09-07 最新：吸引で奥行きが潰れる問題を最小修正

- `particle_storm_2.py` の手の力だけZ成分を0にし、XYへの効き方と既存Zノイズ/速度/減衰/境界は維持した。製品変更は1行と説明コメントのみ。
- 実コールバックの3分相当数値試験でZ幅を維持。新3試験と全回帰229件は失敗なし（環境別のskipあり）。原因・測定値・試験条件は `PARTICLE_STORM_DEPTH_FIX_20260907.md`。
- 修正後の実画面の厚みと操作感は人間確認待ち。以下の成人基本操作・30分は過去版の結果として保持する。
- 過去のタスクバー未解決記載は履歴。後続 `TASKBAR_MVP_FIX_20260907.md` で修正・Storm目視合格済み。左右の操作不能帯、USB自動復帰、子供/実2人/12時間は今回のZ修正と別に保留。

## 04:10 最新：成人の両手同時操作OK

- ユーザーが、離した両手で開掌（水色・反発）と拳（赤・吸引）が同時に反応し、左右の役を入れ替える確認に `ok` と回答。成人一人の両手として合格。2人同時・交差・遮蔽を確認した結果ではない。
- trial040343は担当エージェントが次の未確認項目へ進むためManager終了を要求し、04:10:36にoperator_quit、exit0、403.0秒、promotion_count1で終了。対象4 PIDと共有メモリ不在を確認。これは同じManager内の異種切替合格ではない。
- 中央、映像内の四隅、退出再入場、両手の成人確認は完了。左右各240pxの操作不能帯の対応方針、タスクバー露出のMVP必須修正、終了例外、子供/2人/現場/切替/長時間は保留。

## 04:05 最新：退出再入場OK、両手確認待ち

- ユーザーが、両手を10秒外すとカーソルが消え粒子は継続し、開いた片手を中央へ戻すと追従/反応が再開する確認に `ok` と回答。準備完了と別の結果報告として、成人の退出再入場合格を記録する。
- 同trial040343を継続。86.141秒時点camera failure/reopen0、promotion_count1、scene_exitなし、直近描画26.3fps、検出/描画エラーログなし。
- 次は生映像の中央左/中央右に手を離して配置し、片手を開掌（水色・反発）、もう片手を拳（赤・吸引）にする両手の同時反応を確認する。重ねたり交差させず、左右の役を入れ替えて見る。結果待ちであり両手/2人の合格にはまだしない。
- 左右の操作不能帯は対応方針未確定。タスクバー露出はユーザー指定のMVP必須修正。今回の退出再入場合格とは別に保持する。

## 04:04 最新：準備OKを受領、退出再入場は未判定

- 「準備ok」は試験準備完了であり、退出再入場の合格として扱わない。
- 前trial034658は04:02:08にduration_reached、exit0、900.843秒、promotion_count1で終了。対象の実行プロセス/共有メモリ不在を確認し、同条件で再表示した。終了時のKeyboardInterrupt/ConnectionAbortedErrorは引き続き保留。
- 現trial `kids_trial_20260907_040343_885724400`。FIRST_FRAME3.672秒、露出-4/zoom176を一時適用、保存なし。Manager2404/scene17432/wrapper20300/外側27288。15分上限（04:19頃自動終了）。中央で退出10秒→再入場の一まとまりだけ結果待ち。左右帯とタスクバーの必須/保留事項は維持する。

## 03:59 最新：タスクバー露出をMVP必須修正として登録、確認を続行

- ユーザーが観客画面下端にWindowsのメニューバー（タスクバー）が見えると報告。「いつ直してもいいが、mvpのmust。次へ進め」と指示。原因は未調査。MVP完了前に、Xiaomiの映像を表示中はWindowsのタスクバーが観客画面へ露出しない状態にし、Acer DISPLAY1で操作を続けられることを実機確認する。自動切替/手動操作後の再出現も確認対象。
- 現在は表示設定・OS設定・コードを変更しない。これは今回のユーザー報告に基づく未解決の必須項目であり、全面表示を合格にしない。
- 左右各240pxの操作不能帯も未解決のまま保持。帯をなくす質問への確定回答とは解釈しない。ユーザーの「次へ」に従い、表示範囲に依存しない退出10秒→中央への再入場だけを確認する。退出/再入場の合格を表示範囲やタスクバーの合格に流用しない。
- 同trialは718.453秒時点で同一scene、promotion_count1、camera failure/reopen0、描画29fps、運転中の検出/描画エラーログなし。15分上限のため04:02頃自動終了予定。

## 2026-09-07：左右の操作不能帯をユーザーが指摘（優先保留）

- 四隅OKの後、ユーザーが「左右両端は粒子以外が存在できない空間では」と指摘。退出再入場の結果ではない。退出再入場確認は保留し、表示範囲の判断を先に行う。
- 現コードと合成入力で再現：物理入力1280×720→CameraResizingProxyで640×480→prepare_camera_frameで1920×1080へ等比配置。背景の有効範囲はx=240〜1679、y=0〜1079、左右各240pxは映像なし。normalized_to_stageの到達範囲も同じ。粒子描画は画面全体なので両端は粒子のみの帯になる。
- 合成入力による実関数の検証結果：`test_reports/storm_camera_extent_20260907.json`。カメラ/ウィンドウを追加で開いていない。既知の物理入力と表示サイズを使った経路再現であり、画面キャプチャ測定ではない。
- 先の四隅OKは生映像内の位置一致に限る。ディスプレイ全域の操作を合格にしない。16:9→4:3の変形もあり、ネイティブ縦横比は維持されていない。
- 最小案は検出幅640を維持し、入力の16:9に合う高さ360と対応layoutで背景・検出の範囲を合わせること。左右の帯を残す意図か、画面全体を操作範囲にする意図かをユーザーに確認してから扱う。今は映像/実装変更なし。退出再入場/両手と全画面合格は保留。

## 2026-09-07 03:49：四隅OK、退出再入場待ち

- ユーザーが四隅での指先と水色カーソルの位置一致に `ok` と回答。成人の今回の照明・距離条件で合格。次は手を10秒外した時のカーソル消失・粒子の継続、片手を戻した時の追従再開のみを確認する。退出再入場・両手はまだ未確認。
- 同じtrial `kids_trial_20260907_034658_174238400` を継続表示。126.25秒時点camera failure/reopen0、scene_exit記録なし、promotion_count1、描画約27.16fps、読取範囲の検出・描画エラーなし。設定とコードの変更なし。

## 2026-09-07 03:47：残る四隅確認を開始

- 中央の描画・開掌反発・拳吸引・30分の既存結果は保持。資料に専用の合格記録がない四隅、退出再入場、両手を未確認として順に扱う。
- まず四隅の指先とカーソルの一致だけを提示し、ユーザー結果待ち。手首まで入れた開掌を映像内の左上→右上→右下→左下にゆっくり移動。水色カーソルが映った人差し指先に重なればOK。端に近づくほどずれる、反転、検出が途切れる場合はNGとして場所と症状を記録する。
- 起動コードの追加変更なし。trial `kids_trial_20260907_034658_174238400`、FIRST_FRAME3.672秒。露出-4/zoom176一時適用、保存なし。15分で自動終了。
- 旧Spider終了後のPID/共有メモリ不在を確認して起動。これはManagerを一度終了した別試験であり、同じManager内の異種シーン切替反復合格ではない。

## 判定

`particle_storm_acer.py` は、成人による基本操作と30分単一シーン試験を通過したMVP候補とする。子供、2人同時、暗所、12時間、本番採用は未確認であり、Human Check Requiredのまま残す。

中央の生映像が粒子を隠していた原因は、背景planeが深度を書き込み、後段のparticle sceneが同じdepth bufferを使っていたことだった。背景の色は維持したまま `MeshBasicMaterial(..., depth_write=False)` とし、背景が粒子を遮らないようにした。修正はcommit `c258d6c` で候補ブランチへpush済み。

## 実機結果

- 構成: Acer Windows 11、C922n、Xiaomi拡張画面 `DISPLAY5`
- trial: `test_reports/kids_trial_20260906_222052_316031000`
- C922n診断: 1280×720、30fps、MJPG、実測29.99fps
- 比較時設定: 露出-4、zoom 176を一時適用。JSONへは保存していない。
- `FIRST_FRAME`: scene開始要求から約3.55秒、Manager開始から3.781秒
- 人間確認: 生映像の上に中央の粒子が見える、開いた手で粒子が押し広げられる、拳で赤いカーソルになり粒子が引き寄せられる、の3点をOKと確認した。
- 連続試験: `operator_quit`、exit 0、`trial_elapsed_s=1809.953`。30分を超えた。
- カメラ: read failure 0、reopen 0、`last_error=null`、最大frame gap 0.109秒
- SceneMetrics 180件: 19.24～30.00fps、中央値25.96fps
- warm-up後のscene working set: 610,750,464 → 575,913,984 bytes、範囲562,233,344～625,815,552 bytes
- warm-up後のscene handles: 1214 → 1211、範囲1209～1214
- warm-up後のManager working set: 190,021,632 → 184,188,928 bytes
- warm-up後のManager handles: 925 → 918、範囲917～925
- 終了後: 対象PIDとManager所有共有メモリの残留なし
- 終了ログ: rendercanvas描画callback中の`KeyboardInterrupt`と`Runner ERROR notification failed: ConnectionAbortedError`がある。運転中障害ではなく終了要求時の記録だが、完全無エラーとは扱わない。

## 共通経路の監査

本番候補のAcerラッパーと実体、root直下のPython、Manager、Scene Runner、Shared Camera、Spheres専用workerを横断確認した。

既に次の最小共通経路が成立している。

1. `manager.py`の`SharedCameraRelay`が物理カメラを一元所有する。
2. Managerが共有メモリ情報を子シーンへ環境変数で渡す。
3. 各本番候補が`display_utils.open_camera()`から共有カメラへ接続する。
4. `prepare_camera_frame()`が左右反転、アスペクト比維持、余白付きstage画像を同じlayoutから作る。
5. `normalized_to_stage()`が、表示背景と同じlayoutでMediaPipeの正規化座標を画面座標へ変換する。

Dots、Grid、Mandala、Fractal、Particle Storm、Saturn、Spiderはこの経路を使う。Spheresも専用worker内で同じframe preparationと座標変換を使う。

## 共通化候補と今回の判断

追加候補は次のとおりだが、MVP前には実装しない。

- Pygame群に重複するBGR→RGB→Surface変換
- MediaPipe SolutionsとTasksの結果adapter
- カメラ読取失敗時の背景消去、前frame保持、scene終了の扱い
- Pygame、pygfx、Spheresの背景透過率制御
- Particle StormとSaturnに重複する画面座標から3D world座標への変換

理由は、重複行数より作品固有差の方が大きいためである。最大手数、検出閾値、pinch・拳・開掌の意味、2D／3D投影、Pygame透過canvas、pygfxのdepth、Spheresの加算合成、カメラ欠損時の継続方針が異なる。共通化すると合格済みシーンを含む再検証が必要になり、MVP完成を遅らせる。

Particle StormとSaturnはscene側で640×480を指定する。`CameraResizingProxy`が共有1280×720を640×480へ変換し、背景と検出座標は同じ変換を通るため位置は一致する。一方、元映像の縦横比が変わる可能性がある。Particle Stormは現在の正常基準を維持し、Saturn実機試験で横伸びや余白を目視してから個別判断する。

未採用の旧シーンには、独自mirror、resize、座標式、直接`VideoCapture`が残るものがある。候補に選ばれた時点で、既存共通経路へ最小限合わせてから実機試験する。

## 残る確認と次工程

Particle Stormには次を残す。

- 2人または両手で二つのカーソルと反応が独立すること
- 子供が開掌と拳の因果関係を理解できること
- 会場照明と距離で検出が途切れないこと
- 12時間連続試験
- 終了時のrendercanvas割り込み例外の扱い

残りのシーンは共通化作業を挟まず、既存経路を維持してpreflight、中央と四隅の座標、操作、退出再入場、複数人、30分、終了残留の順に個別確認する。次はParticle Stormと同じ依存環境を使うSaturnを優先するが、背景を同じ3D scene内へ置く構造はParticle Stormと異なるため、深度設定を横展開しない。
