## 最新：終了後の状態報告を受け、診断表示を再準備（2026-09-07 06:59）

ユーザーはWindowsデスクトップ・C922消灯の状態から変わらないと報告。旧run064632の終了後の状態と一致し、再生成直後の人間目視は未確認として保持。新run test_reports/usb_stream_isolation_20260907_065838 を開始。外側14732/実PID18364、openedと非黒フレームを確認。露出-4/zoom176/1280x720/DSHOWを維持。今回は返信待ちのため診断だけ30分上限（約07:28:38、native呼出しの停止保証ではない）へ延長。製品コード・保存設定変更なし。新たな起動を旧runの同一プロセス再生成の目視合格に代用しない。

次はUSBを触らず、Xiaomiの生映像が動きC922が点灯するかだけ確認して待つ。まだ新runで抜差し・probe・reopenは行っていない。前回実機のread=True/retrieve=Falseとカメラのみ再生成後の数値復帰の根拠は維持。子供と実2人確認は参加者待ち保留。

## 診断表示終了と人間確認の状態（2026-09-07）

ユーザーの「準備ok」は準備完了の意味として受領し、映像復帰・LED点灯の合格とは扱わない。再読取りで診断run064632はelapsed480.422秒にexit0/cleanup_okで8分上限終了済み。直前478.438秒も再生成後の非黒フレーム32枚/2秒だった。現在は診断表示なし。終了前に生映像の動きとLED点灯を目視できたかを一問だけ確認する。新たな起動を同一プロセス再生成の目視証明に代用しない。

## 最新実機結果：API間の成否差と同一プロセス再生成（2026-09-07）

ユーザーはUSBを戻して約30秒（本人推定）後にカメラ起動通知、ランプ非点灯と報告。診断run064632でそのまま比較した。elapsed304.281/306.281秒のreadはTrue・非空・全黒・各1.000秒。直後305.281/307.297秒のgrabはTrueだがretrieveはFalse、非空全黒バッファあり、各約1秒。正常時は両API成功だった。同一不良captureで2組の結果が一致し、取得失敗をreadが成功として渡す経路を実機で確認。黒画素のしきい値は不要。

続いて明示した診断コマンドで既存_reopen_captureを一度だけ実行。実PID21832維持、327.484秒要求→331.688秒成功（4.204秒）、新capture object、attempt1、release_errorなし。露出-4/zoom176をその場で再読取り確認。次のsampleは非黒フレーム、33枚/2.047秒、read約0.047秒。アプリ全体の再起動なしでカメラだけ再生成すると数値上復帰した。人間にはXiaomiの生映像の動きとC922点灯だけ確認して待つ。まだ人間の復帰合格、自動復帰合格ではない。製品コードは未変更。診断8分上限約06:54:32。

次の最小修正候補は物理DShow取得のgrab/retrieve両方の成功判定（定常取得・初回制御・FPS診断）と、約1秒/回の失敗で30回閾値が30秒を超える点の検討。自動復帰の実装・回帰・抜差し合格は未完了。子供確認と実2人操作は別の参加者待ち保留。

# USB自動復帰NG：原因照合と次の診断

2026-09-07。製品基準は候補ブランチ `68c7e22`。本資料の作成では製品コード・保存設定・OpenCV環境を変更していない。子供確認と実2人同時操作は、それぞれ参加者待ちのHuman Check Required。

## 結論

確定したアプリ側の原因は、黒い画像を伴う読み取り成功で連続失敗数を0へ戻し、カメラ再生成へ進まないこと。下層の最有力原因は、OpenCV 4.12の `read()` がDirectShowの `retrieve()` 失敗を成功扱いへ変えてしまう経路である。実機で `read()` と `grab()/retrieve()` を同じ失敗ストリームに対して比較するまでは、この下層原因を確定としない。

「カメラオン」通知は復帰成功の証拠にしない。通知の発信元・時刻は未確認。ランプ非点灯と映像NGはユーザー観測として別に保持する。

## 実機の事実

- 061447 trialと062005 trialの2回とも、正常な映像からUSBを5秒抜いて同じポートへ戻す手順で映像が戻らないとユーザー報告。両試験とも取得失敗累計2、reopen0、同一Manager/sceneが継続。
- 共有メモリの異なるframe_idを4秒間調べ、初回5枚・2回目4枚が全画素0。画像保存なし。フレーム更新約1fps。2回目のシーン描画は54.42fps、手検出0。描画の停止ではない。
- 両回ともDirectShow列挙にC922が存在。列挙は存在確認でありストリーム供給の証明ではない。
- 初回の通常終了・プロセス/SHM解放後に再起動すると、カメラ診断29.99fps、非黒の数値フレーム、ユーザーによる生映像・指反応の復帰OK。これは再起動による回避であって自動USB復帰合格ではない。
- 2回目は06:35:14にduration_reached / exit0 / 900.890秒 / promotion1で予定終了。今回の再読取りで対象Manager/scene入口の稼働なし・SHM不在を確認。予定終了をカメラ復帰と数えない。

ローカル根拠：`test_reports/kids_trial_20260907_061447_164283500`、`kids_trial_20260907_062005_671497100`、`usb_frame_numeric_20260907.json`、`usb_second_failure_numeric_20260907.json`、`usb_restart_numeric_20260907.json`。人物画像の追加保存はしていない。

## コード上の因果関係

1. `shared_camera.py:619` で物理captureの `read()` を使用。620–628は戻り値、形状、バイト長を検証するが画素内容は検証しない。
2. 629–644は黒いフレームでも新しい時刻・frame_id・status2を付けて配信し、連続失敗・last_error・再接続バックオフをリセットする。
3. 601の再取得条件はcaptureなしまたは連続30回失敗。失敗2回の後の成功が続けば条件に到達しない。累計失敗数と連続失敗数は別で、runtime sampleは累計を出力する（runtime_diagnostics.py:162）。
4. 2秒以内の共有フレーム鮮度条件も約1fpsの更新で通る。Spheres側の0.5秒保持期限では間欠的に背景を消す可能性があるが、既に入力自体が全黒であり主原因は描画合成ではない。
5. 既存再生成処理560–585には、所有captureのrelease→C922再列挙→新しいVideoCapture→初回露出/ズーム確認がある。release失敗時は所有を保持して次のopenを抑制する。今回reopen0なので、ここでreleaseに失敗した証拠はない。
6. 認識待ちは0.25〜最大5秒のバックオフがある。今回は入口に達しておらず、待ち時間を増やすだけでは解決しない。消失・再出現を独立に監視する処理はない。
7. 操作パネルのactualはopen時・設定適用時のキャッシュ。USB後の表示値が−4/176でも、その時点のドライバー読戻し証拠にはならない。今回の再起動直後の値確認とは区別する。

## OpenCVの一次資料と未確定部分

実行環境のログはcv2 4.12.0。公開4.12.0実装では `VideoCapture::read` は `retrieve(image)` の戻り値を使わず画像が空かを返し、`retrieve` 自体はバックエンドの成功値を返す。[OpenCV cap.cpp](https://github.com/opencv/opencv/blob/4.12.0/modules/videoio/src/cap.cpp#L512)

DirectShowの `retrieveFrame` は画像バッファを確保してから `getPixels` を呼ぶ。callback経路の待機は1000msで、取得できない場合falseを返す。したがって、バッファは存在するが取得は失敗した状態を `read()` が成功と返す可能性がある。[OpenCV cap_dshow.cpp](https://github.com/opencv/opencv/blob/4.12.0/modules/videoio/src/cap_dshow.cpp#L3352)、[1000ms待機処理](https://github.com/opencv/opencv/blob/4.12.0/modules/videoio/src/cap_dshow.cpp#L1427)

これは約1秒周期の観測と整合する最有力仮説。未取得バッファが常に黒になるとは主張しない。使用バイナリ内部のこの分岐を直接トレースしておらず、ドライバーが実際に黒い成功サンプルを返している可能性も残る。

## 自動確認

- 関連の既存テスト：再接続16、初回制御値7、カメラprofile9、Spheres camera20、計52成功（独立レビューと本線の重複実行は二重計上しない）。モックによる確認でありUSB実機合格ではない。
- 実際の `_capture_loop` に対し、カメラだけを疑似入力化した4ケースを実行。2失敗→黒40枚は40枚配信/reopen0、30失敗→黒40枚はreopen1、失敗なし黒40枚はreopen0、2失敗→有色40枚はreopen0。共有メモリ生成・更新・closeも実処理を使用。
- 診断スクリプト `test_reports/usb_recovery_offline_diagnosis.py` の4ケースの現状照合は成功。`--require-recovery` は既知NGに対する期待を検証するため意図的に失敗（AssertionError: capture再生成0）。修正済み・全テスト成功とは書かない。結果は `usb_recovery_offline_results.json`。
- nativeのUSBタイミング、カメラ通知、LEDは疑似入力では検証できない。最小実機診断へ進む。

## 最小修正案と回帰・回避リスク（未実装）

優先案：次の診断でretrieve失敗が確認された場合、Managerが物理captureを読む箇所だけ `grab()` と `retrieve()` の両方の成功を確認する。取得失敗を画像バッファの存在で上書きせず、既存の未取得通知・連続失敗・release/reopen経路へ渡す。初回制御値確認と起動FPS診断にも同じ誤判定経路があるため対象に含める。共有メモリの読み手や各シーンの描画・座標は変えない。

- リスク：失敗が正しく見えると現30回閾値では1回約1秒の待機が累積し、30秒以内の復帰条件を超える可能性がある。確認後に失敗継続時間の上限を検討する。単に全体閾値を1へ下げると一過性失敗で過剰再生成する。
- リスク：初回フレームの誤成功が消えると、USB再出現直後の未準備をopen失敗として扱う回数が増える。既存バックオフ、終了応答、旧capture解放、新しい露出/zoom/FOURCC保持を回帰する。
- 暗所回帰：正常な黒い取得成功フレームを失敗にしない。黒画素・低fps・手不在だけでは再生成しない。
- 代替案：明確なデバイス消失→再出現に基づいて既存ownerへ再生成要求。追加監視が必要で、短時間の消失見逃し、列挙エラーとの混同、デバイス同定の回帰リスクがあるため上記の小さい案より後。
- 暫定回避：ユーザー確認済みの通常終了→解放→再起動。映像中断があり無人運用を満たさない。強制終了、別アプリのカメラ同時取得、OSやドライバー交換は行わない。

## 準備した実機診断と再開方法

製品とは別の `test_reports/usb_stream_isolation.py` が既存のSharedCameraRelayのopen/reopenを使用。一つの所有スレッドで読み取り・release・再生成を行い、Xiaomiへカメラだけ表示。推論、シーン切替、黒画素による自動再生成なし。制御は診断run内のcommand.txtの明示コマンドだけ。画像保存なし。約8分上限（native呼出しの停止保証ではない）。

旧準備run064523は正常終了/cleanup_ok。最新runは `test_reports/usb_stream_isolation_20260907_064632`。このrunのevents.jsonlでopenedとsampleを確認してから、人間へC922を5秒抜いて同じポートへ戻し「戻した」と答えるよう一まとまりだけ提示し待つ。

回答後にagentがcommand.txtへ `probe` を書くと、同一captureでread→grab/retrieve→read→grab/retrieveを比較する。黒画素の有無だけでなくret・grab・retrieve・所要秒・サイズを取る。次に必要な場合のみ `reopen` を一度だけ送り同一プロセスで既存の再生成経路を検証し、人間へLEDと映像復帰を確認する。`stop` は通常終了。再起動や無限再試行はしない。正常時のprobeは実行し、read/明示grab+retrieveの両方で取得成功を確認。USB後の比較とreopenは未実行で実機結果待ち。

通常のSpheres試験は終了済み。今回の診断をMVPシーン合格、子供確認、実2人操作、12時間耐久へ読み替えない。製品ロールバック不要（製品変更なし）。従来安全基準705b081、今回調査前68c7e22を保持する。
