# リバース2026：iPhone ChatGPTへの引き継ぎメモ

- 引き継ぎID：REBIRTH-20260905-001
- 版：v003（Acer単独開発、一発差し替え、TouchDesigner表示切替を反映）
- 作成日：2026年9月5日（日本時間）
- 最終更新日：2026年9月6日（日本時間）
- 現在の段階：修正計画と本番運用設計の整理。実装前。
- コード・設定の変更：行っていない。
- 実機試験：行っていない。30分試験も12時間試験も未実施。
- GitHubへのメモの保存：ユーザーから、このメモを含む単発のcommit/pushの指示を受けた。自動同期の案は取り消されたままで、仕組みは設定していない。
- 最新の依頼：今回までの追加条件をiPhone ChatGPTへGitHub経由で引き継ぐこと。

このメモは会話を引き継ぐための資料です。「ユーザーが確定した条件」「コードから確認した事実」「Codexの提案」「未確認」を区別してください。提案はユーザーの承認済み決定として扱わず、最新のユーザー指示を優先してください。最初に第11〜15節の2026年9月6日更新を読み、その後に必要な旧監査情報を参照してください。

## 1. 最初に把握してほしいこと

春風2026のKids Disco向けに急いで作った映像・インタラクションの資産を、次のイベント「リバース」で実運用できるように整えたい。

目的は、小学生が自分の動きと映像の変化の因果関係を直感的に理解して遊べること。同時に、サイケデリック／テクノ系の空間にいる大人にも、映像作品として成立する質を目指すこと。

時間が少ないため、全面的な作り直しではなく、使える既存資産を選んで絞る。最優先は、Acer Windows実機での安定動作、無人オート運用と必要時の手動介入の両立。

現在はコードを変更しない。静的監査は完了し、その結果のうち5項目について修正計画を作成した。その後、本番で使うシーン、参加者操作、MacBook操作、Acer上でのリリース差し替え、TouchDesignerとの表示切替まで計画範囲が広がった。いずれもまだ実装していない。

## 2. ユーザーが確定した条件

### 目的・日程

- 開発完了目標：2026年9月8日夜。
- 9月9日：休養と予備日に確保したい。
- 9月10日：出発。
- 本番の実際の稼働時間は未確定。
- 長時間安定運用の最終的な想定は、暫定で「再起動なし12時間」。
- ただし次の確認は、修正後の「30分間の単一シーン」と「Managerによる切替反復」まで。12時間連続試験にはまだ入らない。

### 中核修正の作業範囲

- 現在は修正計画と運用設計だけを作成する。
- 新機能追加、大規模リファクタリング、修正の実装はまだ行わない。
- 対象は前回監査のP1のうち、#2、#9、#1、#3、#6だけ。
- 基本順序は #2 → #9 → #1 → #3 → #6。依存関係で順序を変えるなら理由を説明する。
- START問題は、単なる待ち時間追加ではなく、受信準備完了を確認できるハンドシェイク方式を検討する。
- 物理カメラはManagerが所有する設計を維持する。
- 起動対象は最終的に明示リスト化し、本番で使う *_acer.py だけに限定できる構造を優先する。
- 本番に採用する正確なファイルは、ユーザーの目視選定まで確定しない。
- 基準シーンの第一候補はFinger Colorful Dots系。実ファイル名は次節を参照。

### 認識上の注意

- まだ「Acer実機で長時間安定動作確認済み」と断言できるシーンはない。
- ファイルやラッパーが存在することと、本番実機で安定稼働したことは別。
- 静的に不備が見つかったことと、実機で障害が再現したことも別。
- 音声の重複や曖昧なタイポを新しい決定として扱わない。
- AI側の要約・提案・相槌を、人間による採用決定に置き換えない。

## 3. 対象リポジトリと名前の訂正

リポジトリ：
https://github.com/mindoverdrive/harukaze2026_kidzdisco_windows

監査したmainのコミット：
28f662cdcb4532ca5361f53d81fe8db622c8ae7b

計画作成時にもmainが同じコミットであることを再確認した。以後コミットが変わった場合、この監査をそのまま最新版へ適用したとみなさない。

音声の「アンサー」「アーサー」「Asa」「Arsar」は、この文脈ではAcer／acerを指す。「シーンプロファイルランナー.py」はscene_profile_runner.py、「シェアドカメラ.py」はshared_camera.py。「.2.py」は実際には末尾が _2.py のファイル。

全文を確認した対象は、manager.py、scene_profile_runner.py、shared_camera.pyと、次の7組、合計17ファイル。

| 起動するAcerラッパー | 呼び出す実装 |
|---|---|
| finger_colorfull_dots_acer.py | finger_colorfull_dots_2.py |
| finger_grid_interaction_acer.py | finger_grid_interaction_2.py |
| finger_mandala_acer.py | finger_mandala_2.py |
| fractal_moving_acer.py | fractal_moving_2.py |
| particle_storm_acer.py | particle_storm_2.py |
| saturn_particles_acer.py | saturn_particles_2.py |
| spider_cursor_acer.py | spider_cursor_2.py |

各ラッパーはscene_profile_runner.run_scene()を呼び、runnerはrunpyで同じ子プロセス内に _2.py を実行する。ラッパーと _2.py がそれぞれ別プロセスになる構成ではない。

基準候補の正確な起動対象は [finger_colorfull_dots_acer.py](https://github.com/mindoverdrive/harukaze2026_kidzdisco_windows/blob/28f662cdcb4532ca5361f53d81fe8db622c8ae7b/finger_colorfull_dots_acer.py)。呼び出し先はfinger_colorfull_dots_2.py。finger_colorfull_dots.pyという別ファイルもあるため、試験対象を取り違えない。

補助的にconfig.json、display_utils.py、sakura_transition.py、requirements.txt、sitecustomize.pyも確認した。リポジトリ全体の存在確認では105ファイル、うち89テキストの内容を取得・照合した。バイナリ資産の動作やモデルの有効性を検証したわけではない。

この事実はWindows側Codexが取得したGitHub上の本文についてのもの。iPhone側が同じ本文を読めたことや、Acer上のローカルファイルが同一であることは自動的には保証されない。

## 4. 現在の実装構成

通常の流れ：

Manager
→ SharedCameraRelayが物理カメラを取得
→ 共有メモリへフレームを書き込む
→ 子のAcerラッパー
→ scene_profile_runner
→ 対応する _2.py
→ 共有メモリからカメラ映像を読む

Manager内の認識用スレッドもRelayのフレームを読む。通常のシーン切替では物理カメラを持ち替えず、旧シーンが共有メモリ接続を終了し、新シーンが接続する。

物理カメラの再取得は、カメラ接続障害やManagerの再起動などで問題になる別経路。

## 5. 静的監査の結果：番号を維持する

以下は前回の監査番号。以降の「#番号」はこの表を指す。

| 番号 | 重要度 | 静的監査で確認した問題 | 今回の修正計画 |
|---|---|---|---|
| #1 | P1 | STARTを一度だけ送るため、子のUDP待機開始前なら取りこぼす。起動成功を確認せず旧シーンを終了する。 | 対象 |
| #2 | P1 | 自動選曲がAcer版に限定されず、別版や補助ファイルも選ぶ。 | 対象 |
| #3 | P1 | カメラ再取得失敗後にcap=Noneのままread()を呼び、取得スレッドが終了する。 | 対象 |
| #4 | P1 | 古い共有フレームや認識・描画停止を、正常稼働として扱い得る。 | 対象外。依存と限界を記載 |
| #5 | P2 | Windowsの強制終了を通常切替にも使用し、シーンのfinally/atexitを通らない。 | 対象外 |
| #6 | P1 | 初期化途中・例外時・解放処理中の失敗で、クリーンアップが抜ける。 | 対象 |
| #7 | P2 | 取得スレッドの終了確認前にカメラ・共有メモリを閉じ得る。ネイティブreadのブロックも未解決。 | 対象外。依存と限界を記載 |
| #8 | P1 | カメラ取得Noneの扱いが不統一。Mandala/Spiderは読み取り失敗1回で終了する。 | 対象外。依存と限界を記載 |
| #9 | P1 | Acerの子プロセス設定と、物理カメラを開くManagerの設定が一致しない。 | 対象 |
| #10 | P2 | 共有無効設定でも共有接続する経路、古い接続情報から復帰できない経路がある。 | 対象外 |
| #11 | P2 | Acer版の画面位置と切替演出の位置が不一致。GPU表示補助にもバックエンド依存がある。 | 対象外 |
| #12 | P2 | WGPUの環境変数名・値が公開仕様と異なり、GPU依存バージョンの固定も不十分。 | 対象外 |
| #13 | P2 | Particle Stormの認識タイムスタンプに時計逆行への保護がない。 | 対象外 |
| #14 | P2 | FPS・推論負荷が不統一。Mandalaの120FPS目標やManagerの追加Holistic負荷がある。 | 対象外 |
| #15 | P2 | sitecustomize.pyが読み込まれる条件では、.venv内のライブラリを書き換え得る。 | 対象外 |

### 蓄積について、断定してはいけないこと

- 7シーンの今回読んだコードでは、粒子や配列が無制限に増え続ける経路は確認できなかった。
- Particle Stormは4万粒子、Saturnは4万5千粒子の固定配列。
- Spiderの粒子は減衰して削除される。
- これは「メモリリークがない」という実機検証結果ではない。
- 強制終了するとPythonの終了処理を通らないが、Windowsによるプロセス資源の回収もある。「強制終了＝必ずGPUやカメラが漏れる」と断定しない。
- ネイティブライブラリ、ドライバー、GPU、長時間の発熱・性能低下は実機で未確認。

## 6. 5項目の修正計画案

ここからはCodexの提案。順序、通信方式、設定項目名、タイムアウト、試験回数・数値は、ユーザーがすべて確定したものではない。実装もしていない。

### 推奨する順序案

**#2 → #6 → #9 → #1 → #3**

ユーザー指定の #2 → #9 → #1 → #3 の相対順序を保ち、#6だけを前倒しする。

理由は、#9のカメラ初期化失敗、#1の起動タイムアウト、#3の再接続失敗を試す前に、途中まで取得した資源や起動した子を片付ける基礎が必要だから。#2の明示リスト検証は、カメラを開く前に確認できるため最初に置く。

### #2：起動対象を明示リストに限定する

**修正対象箇所**
- [manager.py L171–223](https://github.com/mindoverdrive/harukaze2026_kidzdisco_windows/blob/28f662cdcb4532ca5361f53d81fe8db622c8ae7b/manager.py#L171) の_scan_and_shuffle_scenes()と、mainの起動前検証。
- config.jsonのシーン選択設定。項目名の第一案はSCENE_PLAYLIST。

**現在の処理フロー**
ディレクトリ内の.pyを走査 → 除外条件を適用 → シャッフル → earth.pyを先頭へ → 順次起動。

**障害が起きる条件**
対象外.pyが同じディレクトリにあるだけで選ばれる。監査したツリーでは49ファイルが条件に一致し、scene_profile_runner.pyも含まれる。

**最小限の修正方法**
- 設定に書かれたファイルだけを使用する。
- エントリは既存の *_acer.py と照合する。
- 存在、重複、対象ディレクトリ内であることを確認する。
- 空・未設定・不正リストは、カメラ取得前に設定エラーにする。
- 自動走査へのフォールバックとearth.pyの強制先頭配置をなくす。
- 最初はリスト順を使用する。
- 本番採用リストは未確定のまま。試験用リストと本番採用決定を区別する。

**修正後の処理フロー**
明示リスト読込 → 全件検証 → カメラ初期化 → 指定順にAcerラッパーを起動 → 対応する _2.py を実行。

**他項目との依存関係**
#9がカメラ設定を確定し、#1が各起動の成功を確認する。起動途中の失敗は#6へ渡す。

**想定される副作用**
リスト未設定の環境では起動しなくなる。従来のランダム選曲やearth.py優先は変わる。これは意図する選択制御だが、設定移行が必要。

**Acer実機での最小確認手順**
試験用に基準ラッパー1本のリストを使う。別に、空リスト、存在しないファイル、補助ファイル、_2.py直指定を与える検証を行う。

**合格条件**
有効なリストでは記載対象だけが起動する。不正リストでは理由付きで停止し、物理カメラや子プロセスを作らない。リスト外への自動切替がない。

**失敗時に取得するログ・観測値**
要求されたリスト、解決後の一覧・順序・絶対パス、検証結果、実際の起動引数、PID、起動ファイル名、カメラ取得開始の有無。

### #6：初期化途中・例外時のクリーンアップを揃える

**修正対象箇所**
- [manager.py L528–626](https://github.com/mindoverdrive/harukaze2026_kidzdisco_windows/blob/28f662cdcb4532ca5361f53d81fe8db622c8ae7b/manager.py#L528) のmain。
- SceneManager.cleanup()、子プロセスの登録・終了待ち、HeadClapMonitorの終了処理。
- SharedCameraRelayの初期化・start()失敗時の処理。
- 各 _2.py の資源取得と解放。特にFractal/Spiderのループ末尾だけの解放、GPU2シーンのコンストラクタ失敗。
- #1で導入する制御接続も、この終了経路へ組み込む。

**現在の処理フロー**
カメラや監視・子を作る → 後からtry/finallyに入る → 終了時は解放を順番に呼ぶ。途中の初期化失敗ではfinallyを通らず、解放1件の失敗で後続が抜ける。

**障害が起きる条件**
カメラ開始、モデル生成、ウィンドウ生成、Popen、初回起動などが途中で失敗する場合。さらに解放関数自体が例外を出す場合。

**最小限の修正方法**
- 所有する資源の参照を最初にNone等で初期化する。
- Relayの生成とstart()を分け、start()失敗時にも生成済みRelayを参照できるようにする。
- 資源取得開始から例外処理の対象にする。コンストラクタ内部の途中取得も扱う。
- 子はPopen成功直後に管理対象へ登録する。
- 解放を個別に保護し、一つ失敗しても残りを試みる。
- 解放を複数回呼んでも壊れないようにする。
- 元の例外と解放時の例外を別々に記録する。
- プロセス待機に期限を設け、終了できなかったPIDを記録する。
- 2Dのpygame、MediaPipe、カメラ接続、GPU系のcanvasなど、所有する資源を各シーンの終了経路へ揃える。
- 通常のシーン切替を正常終了要求へ変更する#5全体は、この計画に含めない。

**修正後の処理フロー**
資源取得と所有者登録 → 運転、または例外 → 監視停止・各子の終了処理・Relayの解放・画面/制御接続/ログの解放をそれぞれ実行 → 成否と残留を記録。

**他項目との依存関係**
#9、#1、#3の失敗試験の前提。#5の外部強制終了や、#7のネイティブread停止・スレッド競合まで解決したとみなさない。

**想定される副作用**
これまで隠れていた終了エラーが表面化する。二重解放を避ける状態管理が必要。GPUライブラリ内部を一括で破棄する大きな変更は避け、所有しているcanvasや参照の解放に絞る。

**Acer実機での最小確認手順**
通常終了を反復する。加えて検証用ハーネスで、Relay生成後、シーン初期化途中、初回表示前、一つの解放処理に失敗を与える。

**合格条件**
通常終了とPython例外ケースで、対象の子PID・ウィンドウが残らず、次のManager起動でカメラを取得できる。解放1件の失敗後も、他の解放を試みた記録がある。終了不能な資源があれば合格にしない。

**失敗時に取得するログ・観測値**
最初の例外の完全なtraceback、失敗段階、資源の生成・解放結果、親子PID、終了コード、終了所要時間、残留ウィンドウ、ハンドル数、カメラ再取得結果。

### #9：物理カメラの設定をManagerで一度だけ解決する

**修正対象箇所**
- [manager.py L528–541](https://github.com/mindoverdrive/harukaze2026_kidzdisco_windows/blob/28f662cdcb4532ca5361f53d81fe8db622c8ae7b/manager.py#L528) のRelay設定。
- [scene_profile_runner.py L8–31](https://github.com/mindoverdrive/harukaze2026_kidzdisco_windows/blob/28f662cdcb4532ca5361f53d81fe8db622c8ae7b/scene_profile_runner.py#L8) のプロファイル適用。
- shared_camera.pyのchoose_camera_index()と管理下の共有接続経路。
- 必要な範囲のdisplay_utils.pyの設定読込・open_camera()。

**現在の処理フロー**
Managerがconfigで物理カメラを取得 → 子ラッパーがAcer用環境変数を設定 → 子が共有メモリへ接続。子のカメラ設定は、既に取得済みの物理カメラには反映されない。

**障害が起きる条件**
ManagerのDirectShow固定と、Acerラッパーのany/フォールバック有効などが食い違う場合。既定のOpenCV番号0が名前照合や別の番号指定を上書きする場合。

**最小限の修正方法**
- Manager起動時に運用プロファイルとカメラ設定を一度だけ解決する。
- 優先順位案は「明示環境指定 > configの明示値 > 選択プロファイルの既定値 > 内部既定値」。
- 番号指定と名前選択の扱いを明示する。未指定のOpenCV番号はNoneにし、内部既定値0をユーザーの明示指定と混同しない。
- 相反する明示指定がある場合は、選択理由を曖昧にせず設定エラーとして扱う。
- 解決値と、実際に開いた機器・バックエンドを記録する。
- Manager配下では、子は指定された共有メモリだけに接続する。接続失敗時に子が物理カメラを開かない。
- 管理下のrunnerがカメラ設定を再上書きしないようにする。表示プロファイルの変更は別扱い。
- 具体的な機器番号やバックエンドの採用値はAcer実機確認後に決める。

**修正後の処理フロー**
設定・選択方法の検証 → Managerが物理カメラ取得 → 実際の取得値を記録 → 共有接続情報を子へ渡す → 子は共有映像だけを使用。

**他項目との依存関係**
#2で対象起動を限定し、#6で取得失敗を片付ける。#1の初回起動、#3の再取得はこの解決済み設定を使う。管理下での物理フォールバック防止に限定し、#10のセッション復旧全般へ広げない。

**想定される副作用**
従来、別番号や別カメラへのフォールバックで偶然動いていた環境は、明確な設定が必要になる。カメラ名・番号と実物の対応は実機で確認が必要。

**Acer実機での最小確認手順**
指定カメラで起動し、選択理由と実映像を照合。明示設定の上書き、無効な指定、子側の共有接続失敗も確認する。

**合格条件**
正常時の物理カメラ所有者はManagerのみ。子は同じ共有メモリへ接続する。無効指定は理由付きで失敗し、別カメラや子の物理取得へ黙って切り替わらない。

**失敗時に取得するログ・観測値**
設定項目ごとの入力元・解決値、機器名・番号、試行/実取得バックエンド、要求/実解像度・FPS・FOURCC、Manager PID、共有メモリ名、子の接続先。環境変数全体ではなく、この動作に関係する項目を記録する。

### #1：STARTをハンドシェイク方式にする

**修正対象箇所**
- [manager.py L252–264](https://github.com/mindoverdrive/harukaze2026_kidzdisco_windows/blob/28f662cdcb4532ca5361f53d81fe8db622c8ae7b/manager.py#L252)、L351–445の制御通信・先読み・切替。
- scene_profile_runner.pyの管理下起動処理。
- 各 _2.py の初回フレーム処理完了通知。描画アルゴリズムは変更しない。

**現在の処理フロー**
Popen → STARTをUDPで一度送る → 子は後から受信ポートを開く場合がある → 受信できなければ無期限待機。切替ではSTART後0.25秒で旧シーンを終了する。

**障害が起きる条件**
子の起動が遅い、START送信が先行する、初期化が0.25秒より長い、モデル/カメラ/GPU初期化に失敗する場合。

**最小限の修正方法：第一案**
既存のrunnerに起動同期をまとめ、localhostのTCP接続1本で合図を交換する。Python標準ライブラリを使い、新しい常駐サービスは作らない。

Managerは127.0.0.1で待受を完了してから子を起動する。子は管理用接続先とlaunch_idを受け取り、runnerで接続とSTART待機を行う。旧 _2.py のUDP待機と二重待機しないよう、管理用引数をrunnerで消費し、旧--wait経路に入れない。

UDPを維持する案もあるが、その場合は再送、重複処理、ACKの取りこぼしまで実装する必要がある。今回の第一案はTCPとする。まだ採用決定ではない。

| 合図 | 意味 | Managerの判断 |
|---|---|---|
| READY | 制御接続が成立し、子がSTARTを受け取れる | START送信を許可 |
| START | このlaunch_idの初期化を開始する命令 | 受領確認待ち |
| START_ACK | 子が命令を受領した | 初回描画を待つ。旧シーンは終了しない |
| FIRST_FRAME | 対応する _2.py が初回フレーム処理・描画に成功した | 新シーンを稼働対象にしてから旧シーンを終了 |

追加条件：
- launch_idと期待PIDを対応付け、別起動の合図と混ぜない。
- 同じSTARTで初期化を二重実行しない。
- JSON等のメッセージ区切りを定義し、recv1回を1メッセージとみなさない。
- READYは受信準備であり、モデル・カメラ・画面の準備完了ではない。
- FIRST_FRAMEは単なる関数到達やウィンドウ生成で送らず、取得・描画成功を条件にする。
- 初回確認に必要なフレーム番号等の観測だけ追加し、常時の停止監視である#4全体へは広げない。
- GPU系で描画例外を無視した回を、初回描画成功と数えない。
- 期限案：READYまで10秒、START_ACKまで5秒、START送信からFIRST_FRAMEまで30秒。固定待ち時間ではなく、それぞれ成功次第進める上限。
- READY済み先読み子が次の切替まで待つ時間は、起動失敗の時間に含めない。
- 制御接続断や子の終了を失敗として処理する。Managerの操作受付を長い待機で止めない。
- 失敗時は候補子を#6で片付け、生きている旧シーンを維持する。保持できる旧シーンがない初回失敗は、理由を記録してクリーンアップする。
- 同じ切替要求内で無制限に再起動しない。

**修正後の処理フロー**
Manager待受 → 子起動 → READY → START → START_ACK → 共有接続・初期化・初回描画 → FIRST_FRAME → 新シーン確定 → 旧シーン終了。

**他項目との依存関係**
#2の限定されたラッパー、#9の共有カメラ設定、#6の失敗時片付けに依存。#4の継続的な鮮度監視、#5の通常終了方式、#11の演出位置は残る。

**想定される副作用**
新旧シーンが同時に初期化済みとなる時間が増え、RAM・GPU・CPUの一時負荷が増える。画面の重なりやフォーカスも確認が必要。旧PIDを保持することだけで、完全な無暗転を保証しない。

**Acer実機での最小確認手順**
基準シーンのコールド起動を10回。検証用ハーネスでREADY遅延、START後の失敗、FIRST_FRAME未到達、接続断、重複START・不一致launch_idを確認する。続いてManagerで切替を反復する。

**合格条件**
STARTはREADY確認後のみ。旧PIDの終了は正しいFIRST_FRAME確認後のみ。重複実行・無期限待機がない。失敗した候補だけを片付け、旧シーンが生きている場合は維持できる。

**失敗時に取得するログ・観測値**
launch_id、親子PID、シーン名、各合図の送受信時刻、各段階の経過時間、接続断・タイムアウト理由、子のstdout/stderr、初回フレーム番号、旧PID終了時刻、切替中のRAM/GPU使用量。

TCPの区切り・部分受信に関する根拠：
https://docs.python.org/3/howto/sockets.html#using-a-socket

### #3：カメラ再取得失敗後も取得ループを維持する

**修正対象箇所**
[shared_camera.py L397–435](https://github.com/mindoverdrive/harukaze2026_kidzdisco_windows/blob/28f662cdcb4532ca5361f53d81fe8db622c8ae7b/shared_camera.py#L397) の_reopen_capture()と_capture_loop()。失敗した取得試行の解放も確認する。

**現在の処理フロー**
読み取り失敗30回 → 旧capをrelease → 再取得 → 失敗でcap=None → 次周回でNone.read() → 取得スレッド終了。

**障害が起きる条件**
USB切断、再接続途中、選択バックエンドで再取得失敗、読み取りやフレーム変換の例外。

**最小限の修正方法**
- capがない状態と読み取り失敗を分けて扱う。
- 再取得失敗でループを抜けず、停止要求を確認しながら待機を挟んで再試行する。
- 取得・読み取りの例外を記録し、Noneを正常なcapとして使わない。
- 試行失敗時のcapを解放する。取得担当はManager内の同じ1スレッドとする。
- #9で確定した機器と設定を使用し、別カメラへ黙って切り替えない。
- 復旧時に共有メモリ名・サイズを変更しない。
- isOpened()だけで復旧成功にせず、実フレームが入りframe_idが進んだことを確認する。
- 再試行間隔は1秒程度を初期案とし、無待機で回り続けない。
- ネイティブのopen/readが戻らない問題を、Pythonの再試行だけで解決できたとは扱わない。

**修正後の処理フロー**
取得中 → 失敗 → 旧cap解放 → 再接続待ち → 同じ設定で再取得 → 実フレーム確認 → 同じ共有メモリへの書込み再開。

**他項目との依存関係**
#9の所有者・機器設定、#6の解放に依存。#1で再接続前後のシーン起動を検証する。#4の古い映像の正常扱い、#7のブロック/終了競合、#8の他シーンの単発読み取り失敗終了は別に残る。

**想定される副作用**
復旧待ちが増え、診断ログが増える。カメラが長時間不在でもCPUやログが増え続けない間隔が必要。古い映像の表示を全面的に直す変更は今回含めない。

**Acer実機での最小確認手順**
基準シーン稼働中にカメラを外し、再取得失敗まで待って再接続する。3回反復。復旧後にもシーン切替を行う。停止指示への応答も確認する。

**合格条件**
取得スレッドが例外終了せず、再接続後に同じManager PID・共有メモリでframe_idが再び進む。子が物理カメラを取得しない。OSで同じカメラが再認識された後10秒以内の復旧を暫定目標とする。open/readが戻らない場合は不合格・原因切り分けとし、今回直ったとは判定しない。

**失敗時に取得するログ・観測値**
切断/再接続時刻、open/read開始・終了時刻、連続失敗回数、再取得回数・例外、スレッド生存、frame_idと最終フレーム時刻、共有メモリ名、機器/バックエンド、CPU・ハンドル数、復旧までの時間。

## 7. 修正後の基礎試験案

以下は実施していない。30分という長さはユーザー指定。その他の回数・数値はCodexの暫定案であり、測定前の本番合格値ではない。

### 基準シーン

第一候補をfinger_colorfull_dots_acer.pyのままとする。

理由：
- Pygame系で、WGPU初期化の問題を切り離して確認できる。
- 2手認識、固定1,200ドットで、入力と描画の関係を確認しやすい。
- 現行にもatexitによる解放処理があり、#6の前後比較がしやすい。
- 「最も軽い」「既に安定している」と実測したわけではない。
- 過去の監査ではGPU比較候補にparticle_storm_acer.py、CPU負荷比較候補にfinger_grid_interaction_acer.pyも挙げたが、今回の本番採用決定にはしない。

### 試験A：30分間の単一シーン

構成：Manager＋共有カメラ＋基準シーン。監視機能などの設定を記録する。意図的な切替は行わず、同じ稼働シーンPIDで30分間動かす。

見ること：
- 同じManager・稼働シーンPIDを維持する。
- フレーム番号が進み、実際の手の動きに反応する。
- ウィンドウの停止、予期しない再起動・切替、例外がない。
- CPU、Private Bytes、ハンドル数、描画FPSを10秒間隔程度で記録する。
- 発熱やGPUメモリは取得できる範囲で記録する。

暫定合格条件：
- 30分完走、予期しないプロセス終了・再起動・切替0件。
- 意図的切断試験を除き、2秒を超えるフレーム更新停止がない。
- 描画は10秒平均30FPS以上を基礎確認用の暫定下限とする。目標60FPSとは区別する。
- 暖機後にメモリ・ハンドルが増え続ける傾向がない。
- メモリの比較案は5〜10分と25〜30分の中央値。Private Bytesの増加が10%または50MiBの大きい方を超えたら原因調査に回す。閾値内でも継続増加する場合は保留。
- 終了後に対象PID・ウィンドウが消え、次回カメラ取得ができる。

### 試験B：Managerでの切替反復

第一案は、基準シーン1本だけの試験リストで「同じシーンを別PIDで起動し直す切替」を20回行う。本番の別シーンを採用せずに起動・終了の機構を確認できる。

各回、FIRST_FRAME確認、旧PID消失、演出終了を確認してから次へ進む。

暫定合格条件：
- 20回とも正しいlaunch_idでハンドシェイクが完了する。
- 正常切替では旧PIDがFIRST_FRAME前に終了しない。
- Manager PIDと共有メモリ名が維持され、通常切替で物理カメラを開き直さない。
- 先読みを1つ有効にした定常状態では、アプリ管理下のPIDはManager1＋稼働1＋先読み1。演出や旧シーンの終了後に積み上がらない。
- ハンドル・メモリが切替ごとに持続的に増えない。
- 終了後に関連するアプリPIDが残らない。

同一シーンの反復合格は、異なる実装間の切替合格を意味しない。GPU系を含む別シーン間の組合せ試験は、候補を絞った後に別途必要。

### 試験C：失敗経路の最小確認

#1のREADY遅延・初回描画未到達、#3の切断再接続、#6の初期化/解放例外を、それぞれ分けて確認する。30分連続試験の途中に混ぜず、停止原因と再現条件を特定しやすくする。

検証用ハーネスの作成や障害の注入も、現在は未実施。

### 共通で残す記録

- 試験ID、実施時刻、コミット、設定のコピーまたはハッシュ。
- Acer型番、CPU、GPU、RAM、Windows、GPU/カメラのドライバー。
- Python実行ファイルと依存ライブラリの実バージョン。
- カメラ機器・USB接続・バックエンド・実解像度・FPS。
- 親子PID、launch_id、シーン名、起動/終了コード。
- ハンドシェイクの段階別時刻。
- フレーム番号・最終取得時刻、描画FPS。
- Private Bytes、ハンドル数、GPUメモリなど取得できた指標。
- 失敗前後のログと、期待した動作/実際の動作。
- 子のstdout/stderrはメモリに溜めずファイルへ残す案。毎フレームの大量ログは避ける。

これらの合格は、30分の基礎動作についての合格。12時間安定運用、全7シーンの採用、すべての監査項目の解決を意味しない。

## 8. 未確認・未決定事項

- Acer Nitro AN515-58を開発機兼本番機として使うことは確定。CPU、RAM、Windows、GPU／カメラdriverの実状態は実機記録が必要。
- 実機上のコードと監査コミットの一致。
- 実際のPython/ライブラリのバージョン、起動フックの読み込み状況。
- 本番カメラはLogicool C922。Windows上のdevice名、index、USB接続、backendの実測は未確認。
- 本番ディスプレイはXiaomi L32M8-A2TWN。配置、入力mode、解像度、拡大率は未確認。
- 本番採用シーン、順序、手動介入とオート運用の詳細。
- #6を前倒しする計画、TCP方式、各タイムアウト・合格数値の採用。
- 30分/切替試験の結果。まだ試験していない。
- 本番の実稼働時間。12時間は暫定想定。

## 9. iPhone側で次に進めてよい対話

現在の役割は、ユーザーと目的・運用条件・修正計画を整理し、Windows側へ渡す次の指示を明確にすること。

次の点を守る：
- 「コード・設定の変更はまだしない」を引き継ぐ。
- 修正計画の対象は#2、#9、#1、#3、#6に限定する。
- 決定済み条件を繰り返し質問せず、未確認事項から必要なものだけ確認する。
- 追加の技術案は「提案」と記し、採用や実装開始の決定に変換しない。
- リポジトリを自分で読めなければ「Codexが確認した事実」として扱い、自分も実読したと述べない。
- 手の動きと映像の因果関係、大人にも成立する映像品質、無人/手動運用という目的を保持する。
- 時間が少ないので、変更範囲を増やす提案には必要性と影響範囲を付ける。
- ユーザーが中止を指示したら停止し、復唱や確認質問を重ねない。

Windows側へ戻すときの形式案：

> 引き継ぎID：REBIRTH-20260905-001  
> ユーザーが明示した変更・決定：  
> 追加で確認できた実機情報：  
> iPhone側からの提案（未承認）：  
> 次にWindows側へ依頼したい作業：  
> 引き続き行わない作業：

AIの発言と人間の発言を分け、決定がない欄は「なし」とする。次のプロンプトを作る際は、今回のメモの版と、そこから何が変わったかを明記すると引き継ぎやすい。

## 10. 更新状態

ソース修正、アプリの設定変更、実機試験、自動同期の設定は行っていない。今回、このメモをユーザーの依頼による単発のcommit/push対象とする。

メモは手動で引き継ぐ資料。会話終了後やタスク完了時に、自動的に更新・同期される仕組みは設定していない。

## 11. 2026年9月6日に追加された確定条件

### 開発機と本番機

- 開発はぎりぎりまで本番に使うAcer一台で行う。
- Acerを開発機兼本番機として扱い、合格済みの安定版を作業中の候補版で直接上書きしない。
- 最後に一度の操作で候補版へ差し替え、失敗時は直前の安定版へすぐ戻せることを重視する。
- 実機環境はLogicool C922とXiaomi L32M8-A2TWNを含む。正確なWindows表示名、表示モード、拡大率は実機確認対象。
- AcerからMacBook ProをPANで接続して操作できることは必須条件。操作は可能ならブラウザUIにする。接続方式とフォールバックの詳細は未確定。

### 体験とManagerの挙動

- 人が操作していない時間は、自律的に成立する映像を流す。
- 参加者用画面の上部左右へ「前のシーン」「次のシーン」に相当する操作を常設する案を検討する。
- ボタン操作を、人が遊んでいることを示すactivityとして扱う。
- activity中は独立したゲーム系シーンも混ぜて切り替えられるようにする。
- 手動操作では、承認済みシーンを任意指定できるようにする。
- 自動モード向けシーンと、Minecraft等の手動・独立シーンを明示的に分類する。
- シーン切替時の桜エフェクトは内容を一新する。
- inactivityから自動モードへ戻す案は120秒。ボタンのdwell案は約1秒。いずれも実機で操作感を確認して確定する。

### ユーザーが使いたいシーン候補

次は作品・種類の候補名であり、似た実装のうち正確に使うファイルはユーザーが目視して指定する。

- finger_mandala
- colorfull_dots_spheres
- colorfull_wave_dots
- fractal_moving
- finger_colorfull_dots
- otedama系
- minecraft
- polygon_vibes系
- particle_storm
- roulette
- skeleton_glitch系
- earth
- spider_cursor_acer
- saturn_particles
- colorful_tree系。音声・入力時の綴り揺れとして`coloefull_tree`があるため、実ファイル名を照合する。

各候補は「派手でリッチな描画」を目標にする。ただし、子供向けの単純なゲーム画面にはせず、サイケデリック／テクノ空間で大人が見ても映像作品として成立させる。

`finger_mandala`は複数人対応を求める。第一案は最大3人で、人ごとに異なる色を割り当て、同じ人物の左右の手は同じ色にする。他の適切なシーンにも複数人操作を入れるが、全シーンを同じ操作へ揃えず、シーンごとに異なる趣向を持たせる。

### `全シーンを棚卸しして分類`との突合結果

同タスクは、Manager候補50ファイルと除外中の体験・転換用5ファイル、合計55ファイルをソースから確認した。評価順は「小学生への分かりやすさ → サイケデリック空間の大人への伝わり方 → 技術的説明力」。分類は設計上の初期評価であり、その後のユーザーによる「使いたい」指定が優先される。

| ユーザーの候補名 | 棚卸しでの代表ファイル | 棚卸し評価 | 現在の整合した扱い |
|---|---|---|---|
| finger_mandala | `finger_mandala_3.py` | キープ／看板作品 | 最優先候補。現在の`finger_mandala_acer.py`は`finger_mandala_2.py`を起動するため、3人対応案と本番entrypointの対応を目視選定後に確定する。 |
| colorfull_dots_spheres | `colorfull_dots_spheres.py` | ドロップ案 | ユーザーが後から明示的に希望したため候補へ復帰。指の影響が弱いという監査所見を実機目視し、独立scene、idle映像、reworkのどれにするか決める。 |
| colorfull_wave_dots | `colorfull_wave_dots.py` | マージ／ドット遊び | 使用候補を維持。独立sceneにするか、finger dotsのlookへ統合するかを目視で決める。 |
| fractal_moving | `fractal_moving_2.py` | キープ／動く光 | 使用候補。既存Acer entrypointは`fractal_moving_acer.py`。 |
| finger_colorfull_dots | `finger_colorfull_dots_2.py` | キープ／入口 | 使用候補かつ最初の安定性基準scene。既存Acer entrypointは`finger_colorfull_dots_acer.py`。 |
| otedama系 | `modern_otedama.py` | キープ／身体ゲーム | `modern_otedama.py`を代表候補、`otedama_modern.py`は演出素材のマージ案。正確な本番entrypointは未作成・未決定。 |
| minecraft | `minecraft.py` | リワーク／工作室 | 手動・独立sceneの有力候補。まず「空中に置く」を中心にし、長く遊ぶmodeとして扱う案。 |
| polygon_vibes系 | `polygon_vibes.py` | リワーク／触れる空間 | 本体を代表候補とし、`polygon_vibes_face.py`と`pygfx_psychedelic_art.py`の表現を選択的にマージする案。 |
| particle_storm | `particle_storm_2.py` | キープ／宇宙の山場 | 使用候補。既存Acer entrypointは`particle_storm_acer.py`。 |
| roulette | `roulette_game_advanced.py` | リワーク／回す模様 | 使用候補。現コードは当選ゲームより、手の回転で模様を回す作品に近い。用途と本番entrypointを目視後に確定する。 |
| skeleton_glitch | `skeleton_glitch.py` | キープ／踊る本編 | 使用候補。綴りは`skeleton_glitch.py`が正しい。既存Acer wrapperはない。 |
| earth | `earth.py` | リワーク／宇宙への入口・待機 | ユーザーは基本そのままを希望。映像を維持し、操作の複雑さが実機で問題になった部分だけ修正候補にする。 |
| spider_cursor_acer | `spider_cursor_2.py` | キープ／生き物遊び | 使用候補。既存entrypointは`spider_cursor_acer.py`。 |
| saturn_particles | `saturn_particles_2.py` | リワーク／崩して戻す宇宙 | 使用候補。既存entrypointは`saturn_particles_acer.py`。左右の役割の分かりやすさを目視確認する。 |
| colorful_tree系 | `colorfull_tree.py` | リワーク／育てる遊び | 使用候補。リポジトリ上の正確な綴りは`colorfull_tree.py`。既存Acer wrapperはない。 |

棚卸しで強いキープ評価だったが、ユーザーの「使いたい」一覧にまだ入っていない候補は次の3本。自動追加せず、目視候補として提示する。

- `finger_grid_interaction_2.py`：触ると布状の網が引かれ、つまむと切れ、再生する。既存entrypointは`finger_grid_interaction_acer.py`。
- `hands_shake.py`：手同士を近づける共同遊び。複数人体験の候補。
- `spiral_mouth_effect.py`：口を開くと渦と煙が強くなる変身の入口候補。

棚卸しで`scene_profile_runner.py`はsceneから除外し、機材profile付き起動基盤として保持する評価だった。これはP1 #2の明示playlist方針と一致する。`sakura_transition.py`はscene数に含めず章の転換基盤としてキープする評価だったが、ユーザーは切替演出の見た目を一新したい。役割は保持し、視覚内容は再設計候補とする。

棚卸しで提案された体験構造は「入口：指の波・変身 → 本編：触る・描く・踊る → 山場：共同遊び・宇宙 → 余韻：模様・万華鏡」の短い循環と、長く遊ぶ工作室。今回のAUTO／ACTIVE_PLAY／MANUAL分類はこの構造を参考にするが、各sceneの所属はまだ確定しない。

## 12. rebirth2026側から確認した技術的事実

これはWindows側Codexが別タスク`rebirth2026 Sol 再開`と`TD復旧とcanonical実体照合を続行`、および`C:\rebirth2026`のファイルから確認した事実。iPhone側は、自分で同じソースを取得していない限り「Windows側Codexが確認した事実」として扱う。

### MediaPipeとC922

- C922の安定基準として1280×720、MJPG、30fps、露出-5を使っていた。
- カメラ入力は約30fps、Pose／Hand／Face推論は約10fps、Objectは修正後約7.4fps、TD描画は60fpsという別周期で動かしていた。
- 描画周期と推論周期を分離し、遅い推論を表示全体へ伝播させない設計が有効だった。
- Hand＋Faceは5分以上、Hand＋Face＋Objectは10分以上の安定動作履歴がある。ただし今回のKids Disco Pythonシーンや本番12時間試験の合格を意味しない。
- MediaPipe Objectの`Category.index`は`None`になり得る。`int(None)`でcallbackが終了した履歴があり、修正側では`class_id=-1`と`category_name`基準を使った。
- 生存確認には`sender_fps`だけでなく、増加する`camera_frame_id`と最終更新時刻を使う。映像が生きていても`sender_fps=-1`となった履歴がある。
- Poseのみ、Hand、Hand＋Face、Objectという段階起動と、モデル存在・SHA検査が使われている。

### TouchDesignerプロジェクト

- 別リポジトリはhttps://github.com/mindoverdrive/rebirth2026、Acer上の正規パスは`C:\rebirth2026`。
- canonical development TOEは`C:\rebirth2026\touchdesigner\development\rebirth2026_live.toe`だけ。
- runtime候補は`C:\rebirth2026\touchdesigner\runtime\rebirth2026_runtime.toe`。manifest付きで昇格するまで本番合格扱いにしない。
- `start-venue.cmd`は既にBridgeを起動し、ready fileを待ってTouchDesignerと設定されたTOEを起動する。
- venue設定は現在`Mi TV`、display index 1、1280×720、60Hz、fullscreen true。
- 現在のrunnerはTouchDesignerプロセス生成直後に`READY`と表示する。Xiaomiへの実フレーム表示までは確認していない。
- 現在のOperator PanelのFullscreenボタンは状態値を切り替える。現行本線で、Window COMPをXiaomiへ確実に開く処理は未証明。
- TD側ではPython BridgeだけがC922を所有する。Kids Disco Python側ではManagerだけがC922を所有する。二つのモードを同時にカメラ所有者として起動しない。
- TDの出力優先順位は`EMERGENCY RAW > MANUAL > AUTO`。起動状態はSTANDBY、AI OFFを基準にする。
- TD側にはnative-only Auto 88分19秒とscene切替100回の過去合格履歴がある。ただし最新runtimeへの昇格、Xiaomi表示、熱、今回のモード切替は別gate。

## 13. Acer上で一発差し替えする設計案

ここからはWindows側Codexの提案であり、まだ採用確定・実装していない。

### リリース配置

```text
C:\RebirthKidz\
├─ releases\
│  ├─ stable-<release-id>\
│  └─ candidate-<release-id>\
├─ runtime\
│  ├─ current.json
│  ├─ previous.json
│  ├─ acer-production.json
│  └─ scene-manifest.json
├─ logs\
└─ launch\
```

- 安定版と候補版を変更しないディレクトリとして並置する。
- カメラ設定、シーンmanifest、状態、ログはrelease外へ置く。
- `current.json`のような小さな参照だけを一度に置換する。
- releaseにはGit commit、Python版、依存lock、モデルhash、選択した`*_acer.py`と対応`*_2.py`、カメラ／表示profileを記録する。
- 本番差し替え時は`git pull`、`pip install`、モデルdownload、Windows Updateを行わず、事前構築済み候補へ切り替える。

### 差し替えtransaction

1. 安定版を動かしたまま、候補版のmanifest、依存関係、モデル、空き容量、設定、scene一覧を検査する。
2. 安全な待機表示へ移る。
3. 現Managerを正常終了し、子process、window、GPU資源、C922 handleの解放を確認する。
4. `current.json`を候補版へ原子的に置換する。
5. 候補Managerを起動する。
6. Manager ready、増加するcamera frame ID、操作UI ready、最初のsceneの`FIRST_FRAME`を確認する。
7. 全成功後だけ候補を現行版に確定する。
8. 途中失敗なら参照を戻し、previousの安定版を再起動する。

候補版が30分単一scene、同一scene再起動20回、異種scene切替、失敗経路の基礎gateに合格するまで安定版と呼ばない。最終12時間試験は機能凍結後に行う。

## 14. Python映像とTouchDesigner映像の一発切替案

ユーザー要望は、タイミングによりTouchDesigner作品をXiaomiへ全画面表示し、Python映像へ簡単に戻せること。操作入口を次の4つへ揃える案を優先する。

- `1 Python映像を開始`
- `2 TouchDesigner映像を開始`
- `3 待機画面`
- `4 直前の映像へ戻す`

AcerのデスクトップshortcutとMacBookのローカルWeb UIは同じshow-mode supervisorを操作する。

### 表示の役割

- Acer内蔵画面：操作UI、状態、ログ、緊急操作。
- Xiaomi：作品映像だけを枠なし、最前面、cursor非表示で全画面表示。
- ユーザー発言中の「Acerの画面」は、現時点では「Acerから起動してXiaomiへ全画面出力」と解釈している。Acer内蔵画面自体へ作品を出す要求なら、表示profileを分ける必要がある。

### PythonからTDへの切替flow案

1. 承認済みruntime TOE、manifest、Xiaomi、TouchDesigner、portを事前検査する。
2. Python映像を待機へ移し、Managerとsceneを正常終了する。
3. C922と全子processの解放を確認する。
4. Rebirth Bridgeを起動し、camera frameの更新を確認する。
5. 承認済み`rebirth2026_runtime.toe`を一つだけ起動する。
6. `/project1/output/out_display`をXiaomi用Window COMPへ接続する。
7. project identity、Windowの表示先、出力cook／frameの進行を確認する。
8. 全成功後だけ`TOUCHDESIGNER_SHOW`に確定する。
9. 失敗時はTDとBridgeを終了し、Python映像へ自動復帰する。

本番入口は専用shortcutから承認済みruntime TOEだけを開く。Windowsの`.toe`全体を関連付けるとdevelopment、backup、referenceを誤起動できるため採用候補にしない。

### 最小実機gate案

- Python→TD→Pythonを10回反復する。
- 二重カメラ取得、二重TD、残留processがない。
- Xiaomiへ定めた時間内に実フレームが出る。黒画面を成功と判定しない。
- HDMI抜き差し後に表示先を再検出してfullscreenを再適用できる。
- 各モード30分でメモリ、handle、VRAMが増え続けない。
- TD起動失敗、Bridge ready失敗、Xiaomi不在の各経路からPython安定版へ戻れる。

## 15. iPhone側の現在の役割と次の返却形式

iPhone側は、実装開始を指示せず、ユーザーと次の判断を整理する。

1. 候補シーンごとに、使う正確なファイルと用途を目視選定する。
2. 自動向け、activity中、手動専用の分類を整理する。
3. TouchDesignerへ切り替える運用タイミングと、戻す条件を整理する。
4. 上部左右ボタン、dwell、120秒inactivity、桜transitionの希望を言語化する。
5. MacBook UIで必須の操作と、緊急時にAcerで行う操作を分ける。
6. 決定事項だけをWindows側へ返す。提案は未承認として分離する。

Windows側へ返す形式：

> 引き継ぎID：REBIRTH-20260905-001 / v003
>
> ユーザーが新たに確定したこと：
>
> 採用する正確なscene file：
>
> 自動／activity／手動の分類：
>
> TouchDesigner切替条件：
>
> MacBook UIの必須操作：
>
> 未決定事項：
>
> Windows側へ次に依頼する作業：
>
> 実装開始の許可：あり／なし

ユーザーが明示していない内容を「決定」に変換しない。現在、ソース実装、設定変更、TOE操作、実機試験は未実施であり、自動同期も設定していない。
