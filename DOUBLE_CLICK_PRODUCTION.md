# 本番はこのファイルをダブルクリック

`Start Rebirth Production.cmd`

C922nとXiaomiを接続してWindowsを拡張表示にしたら、上記をダブルクリックする。
検証済みPython・17シーン設定で既存の `scripts/start_operator.py` を起動し、ブラウザーで操作パネルを開く。すでに起動中なら、その操作パネルを開き直す。旧 `Start Rebirth Acer.cmd` は単一シーン用なので、本番17シーンには使わない。

- 起動時にPython依存、シーンファイル、操作画面/観客画面の配置をチェックする。C922nの取得は既存Managerが行う。
- 画面構成が不一致なら止めて表示する。Windows設定やカメラ設定、依存ライブラリを勝手に書き換えない。
- ログは `test_reports/production_launcher_*.stdout.log` と `.stderr.log`。
- 操作パネルの終了操作でManagerを終了する。起動用の小さいターミナルは起動完了後に閉じる。
- ブラウザーの配置はWindowsの前回位置に従う。観客側に開いた場合はAcer側へ移動する。

製品基準 `2a6a116` のシーン・Manager・Transition・設定は変更していない。この入口は追加補助ファイルであり、凍結タグには含まれない。停止状態からのCMD実起動とブラウザー配置は下記の完成確認で合格済み。

## 本線での反映確認（2026-09-09）

起動補助8テスト、関連オペレーター20テスト成功。新規起動の既存入口への委譲、起動失敗、90秒タイムアウト時に強制終了しないこと、同時起動・既存ポートの保護、古い認証URLの除外を確認した。

Acer実機ではCMDの `--check-only` が17シーン・依存・2画面構成の検査に成功。稼働中Managerへの実HTTP認証によるパネル再利用も確認し、新Manager起動が発生しないことを検証した（ブラウザー表示は検証時に抑止）。停止状態からの実起動とブラウザーの実表示位置は引き続き未確認。上段の「起動前チェックのみ」はサイドチャット作成時点の記録。

## 完成確認（2026-09-09、上記の未確認を更新）

- 正規の `quit` で終了コード0。HTTP待受停止、Pythonプロセス残留なしを確認後、本番CMDを一度だけ実行した。Explorer上のダブルクリック操作ではなく、同じCMDをPowerShellから呼び出した試験。
- 04:58:59に起動開始。17シーン・DISPLAY1操作用／DISPLAY5観客用の事前検査成功。約9.4秒でFractalのFIRST_FRAME、約11.1秒でREVEALED。起動コマンドは終了コード0、再試行なし。
- 起動後サンプルでカメラ取得失敗0、再接続0、エラーなし。起動時stderrは空。Braveにオペレーター画面が自動で開いた。
- ユーザーがAcer側の操作ブラウザー、Xiaomi側の映像のみ表示（ブラウザー・タスクバーの混入なし）を「ok」で確認し、完成版としてcommit/pushを指示。今回の2条件は合格。
- 根拠ログ（ローカル）: `test_reports/production_launcher_20260909_045859_32560.stdout.log`、同 `.stderr.log`、`test_reports/operator_20260909_045859/runtime.jsonl`。自動画面取得はURL判別の安全確認で停止したため、配置合格の根拠はユーザー目視報告。
- 今回は記録のみ更新。追加の長時間試験や子供・実際の複数人試験を実施した意味ではない。

起動補助に問題が出た場合は重複起動せずログを保存し、正規終了を確認してから凍結済みの `scripts/start_operator.py` を検証済みPythonで直接起動する。製品タグや既存コードを変更する必要はない。
