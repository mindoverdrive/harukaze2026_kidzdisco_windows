# spheres 86590ee 再起動・指反応の目視準備

2026-09-06。ユーザーの依頼は、候補86590eeを確実に再起動し、人間が指の反応と表示位置の一致を目視確認できる状態にすること。17:02:48の新起動は成功し、17:04時点では表示を続けたまま人間の確認待ちで作業を停止した。以下に当時の記録と、その後に届いたフィードバックを分けて残す。

**18時台の更新：暗い条件の中央テストは「反応なし」「白い円も出なかった」。照明を明るくすると「白い円が出る」と復帰を確認した。** 暗い入力46フレームは新規検出器でも全て未検出。ソフト設定は変更せず、今の照明で指先への追従と粒子反応を再確認中で、MVP次工程とDotsへの切替は保留。現在の判定と詳細は [Spheres MVP判定](SPHERES_MVP_REVIEW_20260906.md) を参照し、以下の過去の改善確認を現在の操作合格へ流用しない。

**17時台の状態：ユーザーから「生映像が見えて、指の途切れも減った」と確認を得た。** 修正ソース233aa50による背景の原輝度表示と露出-4／zoom 176の条件での人間の改善確認であり、不検出ゼロ・座標5点一致・複数人・長時間の合格ではない。JSONは未保存。保存入力の48/48フレームで現行設定による手の検出を確認したため、検出ソース・しきい値は維持した。旧Q／9再出現の原因は未解明。詳しくは末尾の追加記録を参照。

## ユーザー申告

状態：**安定性の要確認事項。原因未解明で、今回の目視試験では修正・再現操作を行わない。**

- 終了のためにキーを押すと2回再出現し、3回目で消えたとの申告があった。
- 音声書き起こしは「9」だが、実際の入力がQか数字9かは未確定。
- 各打鍵の時刻、フォーカスがあったウィンドウ、実際に押されたキーは未確認。書き起こしをキーイベントの記録として扱わない。

以下のログと申告は別の根拠として保存する。回数が似ていることから、各打鍵と起動・終了イベントを一対一に対応付けない。

## 旧セッションのログで確認した事実

対象は `test_reports/kids_trial_20260906_155744_219024000/runtime.jsonl` の末尾。時刻はJST。この旧セッションの記録を、新しい86590eeの起動結果へ読み替えない。

| 時刻 | 記録された出来事 |
|---|---|
| 16:49:51～16:49:53 | wrapper 6828／scene 1568がREADY・START・START_ACKを経てFIRST_FRAMEに到達。FIRST_FRAMEの記録値は2.250秒、frame_id 93695。その後、旧wrapper 21108にscene_switchによる停止要求。 |
| 16:49:55～16:49:56 | wrapper 6828のscene_exit後にwrapper 19508／scene 14064が起動し、FIRST_FRAMEに到達。記録値は1.547秒、frame_id 93786。 |
| 16:49:58 | wrapper 19508のscene_exit後、wrapper 29144／scene 14480が起動。READY・START・START_ACKを記録。 |
| 16:49:59 | 最後の候補は `control connection closed before FIRST_FRAME` でFAILED。FIRST_FRAMEはなく、candidate_discardによる停止要求。 |
| 16:49:59 | run_errorの理由は `no running scene after candidate failure: control connection closed before FIRST_FRAME`。 |
| 16:50:00 | run_endはreason=error、exit_code=1、completed_switches=2、completed_promotions=4。 |

この末尾から確認できるのは、シーンの退出と再起動があり、最後の候補が初回フレーム前の制御接続閉鎖で失敗し、Managerがエラー終了したこと。閉鎖した理由とユーザーのキー操作との因果は未確定である。正常終了や、すべてのOS／GPU資源が解放された証拠として扱わない。

completed_switches／completed_promotionsはManagerの切替・昇格集計であり、打鍵回数や画面の再出現回数と同一ではない。FIRST_FRAMEの秒数はscene_control.detail.elapsed_sで、Manager全体の経過時間とは別である。

## 17:02～17:04の新しい起動と人間による目視の記録欄

新起動は17:02:48。HEAD/sourceは `86590ee247cde7a61dcff674eb972b80d6d96a65` で、担当が `git diff --quiet 86590ee` の成功と11ファイルのSHAを確認した。根拠は `test_reports/spheres_retrial_86590ee_source_manifest_20260906.json`、起動記録は `test_reports/spheres_retrial_86590ee_session_20260906.json`。既存Python 3.12.10 venvを使い、プロセス限定で `PYTHONPYCACHEPREFIX` を未存在の専用パス、`PYTHONDONTWRITEBYTECODE=1` として既存pycを使用しない起動にした。

| 確認項目 | 現在の状態 | 残す内容 |
|---|---|---|
| 起動元 | 照合・新起動成功 | 86590ee、試験ID `test_reports/kids_trial_20260906_170252_17028700` |
| 起動の実体 | FIRST_FRAME到達 | Manager 29272、起動launcher 3552、scene 9504、wrapper 22496。FIRST_FRAME 3.687秒／frame_id 106 |
| 表示先 | 窓配置を観測 | SKYで球体窓26347160が1つ、Manager窓12192726が1つ。球体は1920×1080、原点(1920,0)。見切れ・操作位置の目視は別途 |
| 指の反応 | 人間の確認待ち | 指に反応するか、追従するか、遅延や反応の途切れ |
| 表示位置との一致 | 人間の確認待ち | 中央・左上・右上・左下・右下での指と反応位置の一致 |
| 終了時の観測 | 今回は未実施 | 表示継続中。終了時はブラウザ操作画面の「Managerを終了」を使い、Q／9の再現は行わない |

新起動のC922は名前一致・index 1・DSHOW・MJPG 1280×720。診断は2秒59フレーム、29.48fps。露出は既存設定-5を通常適用し、新しい調整は行っていない。17:04:08のsampleは取得失敗0・再接続0・switch 0・promotion 1・error null。17:04:09のrenderは48.75fps、hands 0だった。過去と条件の揃った速度比較ではなく、手の反応や操作一致の合格にも使用しない。

この再起動時点では自動終了・自動切替の試験指定はなかった。人間はまず手首まで掌を映し、中央で人差し指をゆっくり左右・上下へ動かして、白い円と近傍の波・発光が反応するか確認する。中央が確認できた後だけ四隅へ進み、白い円と映っている指先の位置を比較する、という手順を案内した。17:04時点では確認結果が届くまで表示を継続し、追加の自動操作は行わない方針とした。

この資料の編集担当はログ読取とMarkdownの更新だけを行い、新起動の事実は起動担当から受領した。この86590ee再起動時点ではコードを変更していない。Q／9操作の再現と、その原因を推測した修正は行っていない。

回転軸の見え方や過去の手なしFPS改善は、指反応・位置一致・複数人・子供・30分／12時間の合格へ流用しない。Bluetooth PANとMac–Acerネットワーク検証の中止方針は維持する。トークン付き操作URLはこの資料へ記載しない。

## 追加フィードバックと背景表示の修正

ユーザーから「出来てはいるが検出が悪い・生映像が表示されない」と申告があり、主症状は「指を見失う・途切れる」と確認された。露出・ズームUIは接続しており、値は変更していないとの申告。中央と四隅の座標5点一致について、明示的な合格はまだない。これらはユーザーの観察・申告であり、背景表示の合成テスト結果とは分けて扱う。

担当は `colorfull_dots_spheres.py` の背景 `set_alpha(70)` を `set_alpha(None)` にする1箇所だけを修正した。camera RGB、認識入力、操作座標、9,600粒子は維持し、この描画修正ではカメラドライバーの設定を変更していない。この変更は背景の合成時に加わる暗化を外すもので、手の検出閾値を調整する変更ではない。後述の17:27の露出調整は別の操作として記録する。

実際のmain・rendererをSDL dummyと手なし合成snapshotで通した確認結果は次の通り。カメラや物理ディスプレイは開いていない。

| 検証 | 入力RGB | 背景の実描画RGB | 判定 |
|---|---|---|---|
| 修正前 | (90, 60, 30) | (25, 17, 12) | 背景の明るさを保持できず失敗 |
| 修正後 | (90, 60, 30) | (90, 60, 30) | 背景の明るさを保持して成功 |

前後ともFeed・pygameの解放を確認した。根拠は `test_reports/spheres_camera_visibility_before_20260906.json`、`test_reports/spheres_camera_visibility_after_20260906.json`、実行用の `test_reports/spheres_camera_visibility_probe_20260906.py`。合成入力の成功を、Xiaomi実表示で生映像が見えることや検出品質の合格へ読み替えない。

全回帰はPython 3.11で199件／12.810秒、映像Python 3.12で199件／12.571秒、両方exit0。結果は `test_reports/spheres_camera_visibility_unittest_py311_20260906.txt` と `test_reports/spheres_camera_visibility_unittest_py312_20260906.txt`、各 `_exit.json` に記録されている。

ユーザーの許可を受け、担当は17:17:51から既存SHMの入力を2.954秒、56有効フレーム・4スキップ記録した。保存先は `test_reports/spheres_hand_input_20260906_171751_946733`。既存SHMの読取attachとcloseのみを使い、映像は外部やGitへ送信していない。raw入力が暗く、代表フレームでは手全体を確認できなかったため、認識閾値の比較・変更は行っていない。暗さだけを途切れの確定原因にせず、検出問題は未解決として残す。

### 17:23の実機再読み込み

修正ソース `233aa50` を17:23:30にローカルUIのNextで再読み込みした。新scene 28660／wrapper 32904へ交代し、Manager 29272とSHMは維持した。17:23:35にFIRST_FRAME、記録値4.5秒／frame_id 37098。根拠は `test_reports/spheres_camera_visibility_reload_20260906.json`。

17:23:51の記録はrender 50.65fps、hands 1、取得失敗0／再接続0／switch 1／promotion 2／error null。SKYで球体窓6491694とManager窓12192726が各1つ、球体はXiaomiの1920×1080・原点(1920,0)にあり、背景の生映像と白い指先マーカー1つを観測した。人間が見やすさや追従・位置一致を最終承認したという記録ではない。この時点までは露出-5／zoom 176を維持していた。

### 17:27の露出適用と17:28の入力再記録

担当はユーザーへ、露出-4だけを適用し、zoomのチェックを外し、JSONへは保存しない手順を依頼した。ユーザーは実際の値を-4と回答したが、その時点のAPIはseq 1でzoom 176のみ適用、読戻しのactual exposureは-5だった。申告と読戻しの不一致を記録し、原因は決め付けない。

担当が明示された意図に沿って `exposure: -4` だけを再適用し、17:27:52にseq 2のapplied、actual exposure -4、zoom 176、error nullを確認した。JSONへは保存せず、設定ファイルの差分もない。根拠は `test_reports/spheres_exposure_trial_20260906.json`。この露出値の適用確認を、手の検出改善の確認へ読み替えない。

17:28:58、ユーザーのready返信直後に担当が3秒のSHM記録を実行した。48フレーム／12スキップ、保存先は `test_reports/spheres_hand_input_20260906_172858_300128`。先の17:17の暗い入力・手全体を確認できなかった記録とは別の入力で、露出-4適用後かつユーザーが準備した条件の記録である。条件の異なる前後を、露出だけの効果を証明する比較として扱わない。

### 人間による改善確認と17:30～17:31の観測

ユーザーから「生映像が見えて、指の途切れも減った」と回答があった。背景を原輝度で表示し、露出-4／zoom 176にした条件での改善を人間が確認したものとして記録する。背景表示と露出をそれぞれ単独で比較した試験ではなく、不検出ゼロ、中央と四隅の座標5点一致、複数人、子供、単一30分・12時間の合格へは広げない。

| 観測時刻 | render（描画） | camera_update（画像更新） | hands |
|---|---|---|---|
| 17:30:52 | 49.58fps | 15.995fps | 1 |
| 17:31:02 | 48.89fps | 15.70fps | 1 |

同期したManager sampleは取得失敗0／再接続0、frame_id 47878、maxgap 0.079秒。画像更新が約15～16fpsというのは、この露出・入力条件での観測値であり、約49fpsの描画とは異なる指標である。すべてのフレームでの検出や、違う照明条件での性能を証明する値ではない。

### 保存入力48フレームの基準診断

17:28:58の記録を、実運用の `prepare_camera_frame` による左右反転、BGRからRGBへの変換、CPU MediaPipe Handsの順で1回だけ再生した。現行のmodel_complexity 1、max_num_hands 6、min_detection_confidence 0.7、min_tracking_confidence 0.5、static_image_mode Falseを維持した。

- 保存された48/48フレームで手1本を検出し、この系列内の欠落区間は0だった。
- 0・24・47枚目の画像と保存landmarkの重ね合わせを確認し、手全体と人差し指先に対応していた。これは静止入力の対応確認であり、Xiaomiでの座標5点一致の承認ではない。
- 隣接フレーム間の人差し指先の最大移動は46→47枚目で、正規化距離0.0393。CPU推論は中央値31.44ms、最大53.54ms、再生処理全体は約1.97秒だった。カメラ取得や表示を含む実機のフレームレートではない。
- 比較候補のしきい値は試していない。この系列では途切れを再現しなかったため、現行の検出0.7／追従0.5を変更する根拠はなく、維持する。

根拠は `test_reports/spheres_hand_input_20260906_172858_300128/hands_baseline_0_7_replay.json` と同ディレクトリの `BASELINE_RESULT.md`。入力と全21点のlandmark記録はローカルのignored領域に保持し、Gitへ送らない。記録時の12回のスキップは再構成せず、保存された48フレームの順序だけを確認した。速い動作、遮蔽、多人数、過去の途切れの原因を確定する試験ではない。

**この段階の背景修正・短い保存入力の診断は完了した。** 稼働表示を続け、露出-4／zoom 176はJSON未保存の試用値として維持する。設定ファイル `configs/rebirth_spheres_acer_xiaomi.json` の露出は-5のままで、次の新規起動では保存設定が適用される。座標5点一致、子供・複数人、30分・12時間の実機試験はHuman Check Required。旧Q／9再出現の原因も未解明のまま残し、今回の確認中は再現操作や推測による修正を行わない。

背景表示修正のコードの戻し先は `8fe7940`（描画内容は `86590ee`）。戻す必要がある場合は、ローカルUIの「Managerを終了」で終了してから候補ブランチ上で `233aa50` をrevertし、`Start Rebirth Acer.cmd --scene spheres` で起動する。露出だけを元へ戻す場合はローカルUIで露出-5だけを適用する。元の安全基準点 `705b081` は保持し、mainやstableへ統合しない。
