# C922n：初回フレーム取得後の露出戻り

2026-09-06、Acer / C922n（Windows名 `c922 Pro Stream Webcam`）で確認。対象は既存OpenCV 4.12.0.88、DSHOW、MJPG、1280×720、30fps要求、露出-5。パッケージ更新は行っていない。

**以後の優先順位:** ユーザーは部屋の暗さが低fpsに影響している可能性を挙げ、FPSの追加調査を後回しとした。ここまでに完了した観測と指定値補正は記録に残し、次はMacのUIによる現場での調整を優先する。-5や30fpsを本番での固定合格条件にはしない。

## 再現した問題

カメラを開いて露出-5を設定すると直後の読戻しも-5になる。しかし、最初の成功フレームを取得した後は-4になり、実際のフレーム到着は約16fpsだった。設定直後だけの照合では、シーンが使い始める時点の指定値を保証できていなかった。

|比較|フレーム取得後の露出・実測|証跡（ローカル、画像なし）|
|---|---|---|
|従来の初期設定→4秒取得|露出-4、16.15fps。その後手動モード要求＋露出-5で29.74fps|`test_reports/dshow_exposure_mode_probe_20260906.jsonl`|
|取得前に手動モードを明示してから4秒取得|露出-4、16.64fps。同様に取得後の再適用で29.75fps|`test_reports/dshow_manual_before_read_20260906.jsonl`|
|最初の成功フレーム後に露出だけ-5へ再適用|その後4秒で露出-5を保持し29.61fps。ズームの再設定は不要だった|`test_reports/dshow_first_read_restore_20260906.jsonl`|

比較はそれぞれ1回ずつ実施し、同じハンドルを所有するスレッドだけで設定・取得・解放した。再試行や画像保存はしていない。手動モードのgetterはこのバックエンドでは未対応であり、比較で要求したモードの読戻し確認まではできない。機器がモードを保持する可能性もあるため、全モード・全USB環境での因果を確定したとはしない。

OpenCVのDSHOWはopenや形式変更でストリームを開始/再構築する。「最初のreadでストリームが開始する」とは断定しない。報告FPSも要求フレーム間隔に由来するので、到着フレーム数と実時間から計測する。[OpenCV 4.12 DSHOW実装](https://github.com/opencv/opencv/blob/4.12.0/modules/videoio/src/cap_dshow.cpp)

## 最小変更の範囲

実際のDSHOWハンドルで露出・ズームを明示したとき、最初の成功フレーム後に指定値を再確認する。戻っていた項目だけを1回補正し、次の成功フレーム後にも指定値が維持されることを確認する。固定sleepやモード変更ではなく、取得と設定値の一致を条件にする。

初回起動とカメラ再取得で同じ確認を通す。確認中もハンドルを `self.cap` に保持し、失敗時は既存の停止/解放処理が回収する。未指定項目とDSHOW以外のハンドルは変更しない。読戻し不能、初回取得失敗、補正後に再度戻る場合は、設定が有効になったと扱わない。補正を無制限に繰り返さない。

UIの確認値はこの確認後に更新する。毎秒のHTTPポーリングで物理カメラを読まない。運転中のパラメーター変更も取得側の処理へ渡す構造を保つ。

## 実機確認の判定

修正したのは `shared_camera.py` と関連テスト。7テストを追加し、修正前に11のsubtest失敗を再現、修正後は **全84テスト成功（8.704秒）**。初回/2枚目の取得失敗、2枚目で再び戻る場合、未指定/他backend、カメラ再取得、停止中の追加操作禁止、失敗時のハンドル/共有メモリ解放を確認した。構文・diff確認も通過。

実Managerから `finger_colorfull_dots_acer.py` を起動した30秒試験では、露出の-5→-4を検出し、-5への補正と保持を確認した。起動時の実測は **29.69fps**。その後も取得フレーム数は約30fpsを維持した。途中のscene_exit/再起動はなく、`duration_reached / exit_code=0 / completed_switches=0` で終了した。実測試験時間は30.719秒。終了後の当該Python PID残留なし。

- 起動・終了ログ: `test_reports/first_frame_fix_trial.stdout.log`
- 制御・資源・カメラ記録: `test_reports/kids_trial_20260906_093003_197744800/`
- UI追加までの既存チェックポイント: `45c1d29`（77テスト）。本修正はその後の追加。

初回回帰で既存Windows Job試験に単発WinError 5があったが、次の全回帰は通過し対象テストPIDの残留もなかった。原因を今回のカメラ修正と断定せず記録した。

30秒試験後に別作業のTouchDesignerとStreamDiffusionプロセスの起動を確認した。これらは停止せず、競合の可能性がある追加の物理カメラ再取得試験を保留にした。30分単一シーン・20回切替・実USB復帰・子供の操作・Xiaomi・Mac PAN・12時間は未合格。今回の短時間成功を長時間安定性へ読み替えない。
