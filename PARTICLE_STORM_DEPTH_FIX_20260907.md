# Particle Storm：指への吸引で奥行きが潰れる問題の最小修正

## 08:00 人間確認：継続吸引後の奥行き維持OK

- 追加約2分の吸引と厚み維持の確認にユーザーがokと回答。前回から約6分経過し、同trial075145は518.187秒、同一Storm3352/promotion1。中断の申告なし。ユーザーの結果報告として3分相当の継続吸引後の厚みを目視合格とする。ログでジェスチャーの連続保持そのものを測定した結果ではない。
- camera failure/reopen0、直近25.06fps、読取ログに描画/検出例外なし。製品/設定変更なし、記録のみ。
- 今回の修正に関する中央/左右の位置一致、グーパー、継続吸引後の厚みは人間確認済み。次は表示比変更後の上下端の位置一致を30秒だけ確認する。粒径の好みの明示回答、変更後30分/12時間、子供/実2人/USB自動復帰は別に残す。

## 07:54 人間確認：厚みの見え方OK（3分継続は未確認）

- ユーザーが厚みの確認にokと回答。見え方は成人目視OK。ただし再読取ログがelapsed136.312秒なので、3分連続吸引を完了したとは記録しない。冒頭の3分合格という応答はログ照合後に訂正。
- 同trial075145でpromotion1/camera failure0/reopen0、直近24.4fps、読取ログに描画/検出例外なし。新しい実装や設定変更なし。
- 同表示でさらに約2分の吸引後も厚みが残るかを確認して待つ。途中で手を外していた場合は連続3分を合格にしない。子供/実2人/USB/長時間の保留は維持。

## 07:52 人間確認：グー/パーの吸引・反発OK

- ユーザーが中央でグー/パー3回、赤で吸引/水色で反発する確認にokと回答。表示比変更後の成人ジェスチャー切替は合格。3分吸引後の厚みと粒径の好みは未回答。
- 旧trial073818は残り時間不足のため07:51:42にoperator_quit/exit0、promotion1で終了し、対象Pythonと共有メモリ不在を確認。直前777.703秒時点camera failure/reopen0。
- 同条件で新trial `kids_trial_20260907_075145_394133100` を開始。run `particle_storm_mvp_20260907_075144_013367`、Manager21520/Storm3352/wrapper13784/外側10360、FIRST_FRAME3.782秒、露出-4/zoom176一時適用の実値一致、JSON保存なし。08:07頃上限終了。製品コード変更なし。
- 次は中央で拳・赤カーソルの吸引を約3分保ち、最後に少し左右へ動かす。手前/奥の厚みが残ればOK、薄い板へ潰れる場合はNG。まだ人間確認前。

## 07:50 人間確認：中央と左右端はOK

- ユーザーが「中央→左端→右端、生映像が両端まであり指先とカーソルが重なる」確認にokと回答。成人のこの範囲の表示・検出一致を合格とする。上下端・全四隅、粒径の好み、長時間吸引後の厚み、グーパー切替の個別合格ではない。
- 同trial073818、687.171秒時点promotion1、camera failure/reopen0、直近26.26fps、読取ログに描画/検出例外なし。新コード変更なし。
- 次は検出比率変更後のグー/パー切替だけ中央で3回・30秒確認する。赤で吸引、水色で反発ならOK。3分の奥行き確認は未回答のまま保持。現在の15分上限は07:53頃。

## 追加依頼：粒子を小さくし、表示と検出を画面全体へ合わせる

- 最新指示は「やはり、表示も検出もディスプレイに合わせるだけでいい」。途中の「ずれを残す」指示を上書きし、表示と検出を同じ画面全体に合わせる。粒子小型化も維持する。
- 粒径の変更は `PointsMaterial(size=15)` → `size=7.5` の1行。実pygfxの既定はuniformで、このmaterialの値が描画径を決める。`geometry.sizes`のランダム配列だけを縮めても効かないため、描画に使う値を半分にした。粒子数40,000、発光テクスチャ、色、カーソル、XY操作、直前のZ維持修正は保持。
- 原因は1280×720入力→640×480への変形→1440×1080中央配置で、左右各240pxが映像/検出の範囲外になること。Storm内の読み取り幅640を維持し、高さを表示比から算出（Xiaomiでは360）。1280×720→640×360→1920×1080とし、映像と検出/カーソルは既存の同じlayoutを使う。共通関数・物理カメラ設定・左右反転は変更しない。
- `tests/test_particle_storm_camera_extent.py` は実サイズ設定・実前処理・実投影を合成画像で検証する。左右端の色が切れず単一反転後に画面端まで表示され、四隅と中央の検出位置が3種類のZでも画面に一致することを確認。旧640×480では失敗した試験が修正後成功。
- zoom176の光学/デジタル画角外を復元する変更ではない。現在の16:9入力/16:9表示を対象とする。検出高さの変更と変形除去により、端付近の検出とグー/パー判定は実機で再確認する。粒径の好みと奥行き修正後の見た目も人間未確認。
- 旧trial072307は更新のため07:34:43に通常終了、exit0/promotion1、共有メモリ不在。Z修正の人間合格とは扱わない。
- 最終差分で回帰230件：描画Python 15.991秒/失敗0/skip1、Python3.11 12.307秒/失敗0/skip5（実NumPy/OpenCVの4件＋GLFW1件）。途中の粒径のみ版ではPython3.11のHTTP読取りにWinError10053が1件発生し、コードを変えない再実行229件は失敗0。接続中断の原因は未断定として残し、粒径やカメラ修正によって解決したとは記録しない。
- 今回差分の戻し先は `845598a`（Z維持修正と記録まで）。main/stable・安全基準点705b081を維持。
- `845f641` を候補へpush後、07:38に更新版を起動。run `particle_storm_mvp_20260907_073817_276098` / trial `kids_trial_20260907_073818_662974100`、Manager20872/Storm22608/wrapper7520/外側22980。FIRST_FRAME3.688秒、露出-4/zoom176実値一致（保存なし）。26.156秒時点promotion1/camera failure0/reopen0、直近描画29.9fps、読取ログに描画/検出例外なし。
- 07:38:48のWin32確認でXiaomi全域のclient一致、下端3点もStorm。これは映像内容/検出の人間合格ではない。次は中央→左右端へ手を動かし、生映像が端まであり指先とカーソルが重なるかを約1分確認する。最新操作パネルを右側に表示。15分上限で07:53頃自動終了予定。

## 対象と原因

- 本番候補の経路は `particle_storm_acer.py` → `scene_profile_runner.py` → `particle_storm_2.py`。旧 `particle_storm.py` は対象経路ではない。
- `ParticleStormApp.update_physics()` は `delta = h_pos - self.positions` のXYZ差分を正規化し、吸引・反発の両方をXYZ速度へ加えていた。吸引を続けると粒子のZ位置が手の推定Zへ集まり、速度減衰も働いて薄い層へ収束していた。
- 初期Zは±1000に分布。既存 `DAMPING=0.95` は速度にだけ掛かり、Z位置を原点へ縮める処理ではない。XYZの微小ノイズ、±1500を越えた軸だけ反対側へ戻す境界処理がある。寿命・定期リスポーンはない。

## 最小変更

製品変更は `particle_storm_2.py` の次の1行と説明コメントのみ。

```python
# 全ての手の力を合算した後、速度へ加える直前
forces[:, 2] = 0.0
```

- 吸引・反発とも手の力はXYだけに作用する。複数の手でも同じ。
- 3D距離による力の弱まりと近接時の透過率計算は残す。距離計算前にZ差分を消すと遠い粒子のXYへの効き方まで強くなるため、その変更はしない。
- Zは既存の速度・ノイズ・減衰・境界だけで動く。手のZへ集める力がなくなるので、初期の奥行き分布を潰さない。減衰を弱めたりZ専用の新たな力・寿命・再生成を足す必要はなかった。
- 粒子数40,000、サイズ、色の計算、カメラ、座標変換、背景合成、カーソル、Manager、共有処理と他シーンは変更していない。
- XYと透過率の維持は同じ入力状態での計算式についての確認。Z軌跡が変わるため、長時間後の色・遠近感・引き寄せた形まで旧実装と同一になるという意味ではない。

## 自動検証

`tests/test_particle_storm_depth.py` の3試験は実ファイルの物理コールバックと定数を読み、実NumPyで実行。カメラ・MediaPipe・GPUを使わず、既存テストのモックから分離するため子プロセスで実行する。

1. 同じ初期XYZ、速度、乱数で吸引・反発のXY速度/位置と透過率を旧計算式と比較：合格。
2. 吸引・反発・両方の手で90ステップ計算し、Z位置/速度が無操作の場合と完全一致することを確認。非ゼロ初速と境界越えも含む：合格。
3. 256粒子・30fps固定ステップ・180秒相当の連続吸引。Z標準偏差は旧実装 `584.735 → 0.039`、修正後 `584.735 → 585.201`。有限値かつ初期幅の90～110%を維持：合格。

変更前に3試験中2件が失敗し、変更後に全3件が成功した。これは数値シミュレーションであり、実時間3分の描画試験や12時間耐久試験ではない。

| 環境 | 全回帰テスト | 結果 |
| --- | --- | --- |
| 描画用Python 3.12 / 実NumPyあり | 229件、15.484秒 | 失敗0、skip 1（GLFW実窓の明示実行試験） |
| Python 3.11 / 実NumPyなし | 229件、12.387秒 | 失敗0、skip 4（上記1件＋今回の実NumPy3件） |

全テストは環境ごとに順番に実行。独立した読み取りレビューでも今回の差分にブロッキング指摘なし。

## 実機確認と保留

修正コミット `6477ccb` を候補ブランチへpushしてから、Acer/C922n/Xiaomiで起動済み。修正後の見た目は人間未確認。

- run：`test_reports/particle_storm_mvp_20260907_072306_138819`。trial：`test_reports/kids_trial_20260907_072307_553372400`。
- 07:23開始、外側PID17104/Manager25176/wrapper14268/Storm12432。`FIRST_FRAME` 4.078秒。上限15分で07:38頃自動終了予定。USB抜差しや自動切替は混ぜない。
- カメラ起動診断1280×720/MJPG/30.00fps。従来の比較条件、露出-4/zoom176を一時適用し実値一致。JSON保存なし。
- 26.125秒のsampleで同じStorm、promotion1、read failure/reopen0、camera frame382、age約0.015秒。直近描画10秒区間27.96fps、読んだログに描画/検出例外なし。これは短い起動確認であり長時間合格ではない。
- 07:23:39 Win32実測でXiaomiのclient領域1920×1080と一致、下端3点はStorm自身、サンプル位置にタスクバー露出なし。実画面の見た目・奥行きを自動判定した結果ではない。
- Acer側の新しい操作パネルを右側に開いた。終了は同パネルの終了操作を使い、古いタブで操作しない。

- 人間の確認は1まとまり：映像内の中央で拳（赤カーソル・吸引）を約3分保ち、最後に少し左右へゆっくり動かして開掌に戻す。見る場所は指の周囲と手前/奥の粒子。引き寄せ後も厚みがあり、従来のXY追従・反発が使えればOK。薄い板へ潰れる、左右追従が崩れる、開掌の反発が効かない場合はNG。
- 既存の成人基本操作・30分試験の合格は過去バージョンの結果として保持。今回変更後の30分/12時間試験に代用しない。
- 左右の操作不能帯、USB自動復帰、終了時例外は別件として保持。今回のZ修正で解決した扱いにしない。
- 子供確認と実2人同時操作はそれぞれ別のHuman Check Required。成人の両手操作で代用しない。
- 将来案の「画面外へ出た粒子を中心から再発生」は今回実装していない。

## 保存と戻し方

- 修正と自動試験は `6477ccb` として候補ブランチ `codex/rebirth2026-production-candidate` へpush済み。main/stableと安全基準点 `705b081` を維持。
- 直前の戻し先は `8518965`。この修正を戻す場合は修正コミットを通常のrevertで打ち消し、起動中のStormを通常終了して再起動する。強制reset/pushは使わない。
- 通常の起動経路：描画用Pythonで `scripts/start_kids_test.py --audience --scene particle-storm --duration-minutes 15`。JSON保存値は変更していない。
