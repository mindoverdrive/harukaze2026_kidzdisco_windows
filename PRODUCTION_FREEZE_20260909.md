# Rebirth 2026 本番コード凍結

2026-09-09、ユーザー指定により下記の製品コードを本番採用コードとして凍結する。未確認事項をすべて合格とする宣言ではない。

## 固定する対象

- 製品コミット: `2a6a116cde4d829c1e46106745109aef9f5ec306`
- 固定タグ: `rebirth2026-production-freeze-20260909`（上記製品コミットを指す）
- 根拠となる検証記録: `1219bf19e3272812d7c4802e35a7c0d9d75d9c79` の `INTEGRATION_CHECK_20260909.md`
- 作業ブランチ: `codex/rebirth2026-production-candidate`。mainへの統合は行わない。
- 凍結前照合: `2a6a116..1219bf1` の追跡ファイル差分は検証Markdownのみ。製品・設定の未コミット変更なし。別作業の未追跡HTML/調査資料/status_dashboardは凍結対象に含めない。

製品の実装・採用17シーン・Manager・Transition・Next/Back・オペレーターUI・本番設定をこのコミットの内容に固定する。文書追記は製品変更と分ける。今後の製品修正は原因と影響を明記した別候補で検証し、ユーザーの本番採用判断までこの固定タグを移動しない。

2026-09-09追記: ユーザーの「サイドチャットの変更を反映し必要試験後commit/push」の指示で、追加の起動補助 `Start Rebirth Production.cmd` と `scripts/launch_production.py`、操作ガイドを候補ブランチへ収録。既存の凍結製品ファイルは変更せず、固定タグも移動しない。補助入口は凍結タグに含まれないため利用時は候補ブランチの追加分を保持する。検証範囲と未確認は `DOUBLE_CLICK_PRODUCTION.md` を参照。

## 確認済みの範囲

- 全17シーンの統合目視、Next/Backと末尾・先頭の接続。
- 30分34回切替、15分38操作完了。
- 最後の45分は2700秒・74操作。時間枠外でRestartと直接選択を補完し、計画76操作を確認。45分内76操作や連続60分完走とは表現しない。
- カメラ取得失敗・再接続0、Managerのメモリ/ハンドル/GDI/USER推移は今回範囲で安定。過去シーンのPython残留なし。
- Acer操作用とXiaomi観客用の2画面目視、Safe/解除・Restart・一覧直接選択はユーザー確認済み。最新耐久終了後は構成の列挙確認までで、見え方の再確認は未実施。
- 既存回帰テストは製品基準作成時に510件実行、509成功・1スキップ。凍結工程では製品変更がないため再実行せず、差分とタグ参照を検証する。

## 既知事項・未確認・期限

期限は運用上の確認時点。イベントの具体的開催日時は未確定情報として扱う。

| 項目 | 状態・根拠 | 確認期限 / 対応 |
|---|---|---|
| 子供による実施試験 | 今回未実施。成人の目視・操作から合格へ読み替えない | 現場合流後、本格運用前に実施予定 |
| 実際の複数人同時操作 | 今回未実施。1人の両手確認と区別 | 現場合流後、本格運用前に実施予定 |
| Skeletonのメモリ増加 | 初期化後最大約231MiB増。閾値未満だがリーク有無は未確定 | 現場リハーサルで表示継続・重さを確認。異常は記録して採用判断 |
| Skeleton/Spheresの処理FPS | 約8FPSを観測。計測値だけで滑らかさを合格にしない | 観客受け入れ前に見た目・反応遅延を目視 |
| 最新耐久後の2画面・状態漏れ | 配置列挙は一致。見え方・前シーン残像等は未確認 | 現場接続後、観客受け入れ前にNext/Back一往復で確認 |
| 現場の照明・距離・露出・ズーム | Acer室内試験と現場は条件が異なる | 設営後に調整値を記録し、運用開始前に確認 |
| 12時間連続稼働 | 未実施。15分と45分を連続60分・12時間合格としない | 実施する場合は別試験。今回は凍結条件から除外し、未確認のまま運用判断 |
| GPUメモリ | 計測値nullで未評価 | 計測可能な環境で別途。今回の凍結で合格扱いしない |
| 単発アクセス拒否 | 過去に発生、最近の試験で再現なし。原因解決済みではない | 起動時に再発したらログを保存し、起動成功を確認して運用 |
| USB/HDMI抜き差し・OS再起動後 | 今回の最終耐久では未試験。過去の確認と区別 | 設営時に通常起動を確認。抜き差し耐性を今回の結果として保証しない |

本番コードの凍結日は2026-09-09。従前共有の出発予定は9月10日であり、現場確認の具体時刻は別途設定する。未確認項目を消化するために自動で新機能追加や長時間試験を再開しない。

## 起動と現場の確認順序

Acer Windows 11単体で実行・制御・出力する。C922nを接続し、主画面DISPLAY1は操作用、Xiaomi（現在の内部名DISPLAY5）は拡張観客表示。両画面1920×1080、観客画面位置(1920,0)。Mac/PANは対象外。

現在の17シーン用入口は `scripts/start_operator.py`、設定は `configs/rebirth_operator_acer_xiaomi.json`。旧単一シーン用起動手順と混同しない。

```powershell
Set-Location 'C:\Users\go\Documents\ChatGPT\New project'
& 'C:\Users\go\.gemini\antigravity\scratch\harukaze2026_kidzdisco_windows\.venv\Scripts\python.exe' 'scripts/start_operator.py'
```

すでに起動中なら重複起動しない。起動ログに出る最新URLでオペレーターUIへ接続する（認証トークンは起動ごとに異なるため保存文書へ固定しない）。

1. 接続・拡張表示・生映像を確認し、照明と手全体が映る位置を整える。
2. Next/Back一往復、Safe/解除で観客側にデスクトップやタスクバーが出ないことを確認。
3. 子供による操作を実施し、その結果を別記録に残す。
4. 実際の複数人操作を実施し、両手確認と分けて記録する。

検証済み状態へ戻す基準も上記の固定タグ/コミット。未コミット作業のある現用ディレクトリをreset --hardせず、必要時は別チェックアウトへ展開し、本番環境で確認してから差し替える。凍結タグ自体には後日の検証文書が含まれないため、本書と検証記録は候補ブランチ側で併せて保持する。
