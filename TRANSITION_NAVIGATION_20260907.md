# Next / Back と全面遮蔽切り替え

## 今回の完了判定

2026-09-07 17:56頃、ユーザーが最終粒子版について「操作・粒子の落ち方・映像のつながり、全部OK」と回答。Next半秒で離脱キャンセル、約1秒保持と円形進捗で1回切替、Backで戻る、ロゴ・文字を含む粒の隙間から次シーンが現れること、デスクトップ／タスクバー非表示を一括確認した。

最終表示とソースのSHA-256照合は9ファイル全件一致。17:56:54時点で4切替／5表示、C922取得失敗・再接続0、switch error / ERROR 0、stderr空。以後このコードを変更せず、今回の変更だけを1コミットとして候補ブランチへ保存する。確認中の表示は維持する。

本番Pythonの403テスト成功（skip 1）、補助Pythonの396テスト成功（skip 36）。長時間試験の実績は各版の履歴へ分けて記載し、最終粒子版の30分完走、子供本人の操作、二人以上の同時操作、12時間連続運転は今回の合格に含めない。

## 最新の演出方針（過去の波線記録を更新）

粒子版の実装・自動検証完了。本番 Python 3.12 は全403件成功（skip 1）、3.11は396件成功（graphics依存不足等でskip 36）。専用の粒子描画17件、カプセル表示7件を含む。粒子は897個、キャッシュ18枚、合成画を落とす断片510個。1920×1080 dummy描画の粒子＋断片処理は中央値11.45ms／95%点15.77ms（実カメラ・ロゴ・実表示を除くため全体FPSではない）。

最終版は `test_reports/kids_trial_20260907_175518_784909200/` で17:55に起動。stdoutは `navigation_particles_20260907.stdout.log`。初回と次の切替でCOVERED / FIRST_FRAME / REVEALEDを確認、stderr空、ロゴ・粒子の無効化警告なし。実機目視の結果は上の完了判定を参照。

直前の波線＋ロゴ版はoperator_quitで正常終了し、1233.5秒・39切替／40表示・exit 0。終了したManager/overlayの不在を確認して粒子版へ移った。これは粒子版の長時間合格には流用しない。

ユーザーの追加指定により、波線は廃止。画面全体を粒子群が覆い尽くし、その群れがばらばらと下へ落ちて次シーンへ移る表現に置き換える。粒子の具体的なモチーフはまだ未決で、まず抽象的な光粒・断片で動きを確認する。中央ロゴと `KidzDisco Colony` の9パターン、共有生映像、完全遮蔽確認後の差し替えは維持する。Next / Back は透明で浮遊する細長いカプセル形へ変更する。以下の波線の記述や実機結果は、それ以前の版の履歴であり、新しい粒子版の合格を意味しない。

粒が落ちた箇所から次シーンが見えるようにする。次シーンの FIRST_FRAME と旧シーン停止を確認した後だけ、カメラ・粒・ロゴ・文字の合成画を小さな断片へ分けて落とし、隙間を透明色キーにする。準備待ち中は断片化せず完全な不透明面を保持する。合成画はメモリ内の1枚を再利用し、カメラ映像を新しくファイル保存する処理は加えない。入りは約1.2秒フェード、出る側は約1.6秒の崩れ落ちる動きとして試す。

## 基準と対象

- 保護基準は `59f5d46`。今回の開始 HEAD は `0f72a99`。
- Fractal Moving、Finger Mandala 3、Wave Dots、Tree のシーン描画・手操作ロジックは変更しない。
- `manager.SceneManager` は候補選択、起動ハンドシェイク、プロセス所有と終了を担当する。
- `TransitionManager` は観客画面の常設 UI、全面遮蔽の完了確認、解除の完了確認を担当する。
- 物理 C922n は引き続き Manager の SharedCameraRelay だけが所有する。ナビゲーションの手検出は共有映像を読む。

## 操作仕様

- 左上 Back / 右上 Next に人差し指の検知点を約 1 秒保持すると発動する。
- 検知点の周囲に円形の進捗を表示する。途中退出・検出消失でキャンセルする。
- 発動後はボタン領域から指を離すまで連続発火させない。切り替え中は新規要求を受け付けない。
- Next / Back / 許可済みシーン指定は `SceneManager.request_scene()` に集約する。
- キーボードは Manager Control の `n` / `b` / `q`。既存認証付き `/api/action` も `next` / `back` / `select` を同じ経路へ渡す。
- `select` は `{"action":"select","scene":"finger_mandala_acer.py"}` のように指定する。登録外のファイルは起動しない。

## 切り替え順序

1. 明示リストから次の対象を選ぶ。候補は最大 1 つ。
2. TransitionManager が全面遮蔽を要求する。
3. 遮蔽ウィンドウの描画後の `COVERED` 通知を待つ。
4. 候補の `READY` と遮蔽完了が揃ったら `START` を送る。
5. 同じ共有カメラを使った `FIRST_FRAME` を確認する。
6. 旧シーンのプロセス群の終了を確認し、新シーンを昇格させる。
7. 遮蔽を解除する。`REVEALED` まで重複した切り替えを受理しない。

秒数経過を遮蔽完了・描画成功の代用にしない。候補が失敗した場合は候補の終了を確認してから旧シーンへ戻す。終了を確認できない場合は解除を行わずエラーとして扱う。旧 `sakura_transition.py` の自動終了演出は、この経路では使用しない。

## 実機起動

既存の実機用 Python で以下を実行する。

```powershell
python scripts/start_kids_test.py --audience --navigation --duration-minutes 30
```

専用設定は `configs/rebirth_navigation_acer_xiaomi.json`。Fractal → Mandala 3 → Wave Dots → Tree の順で、末尾から先頭へ戻る。Back は逆順。先行プリロードは無効。

## 検証状況

実装後、Python 3.11 / 3.12 で各 349 テスト成功（skip 6 / 1）。SceneManager の順序・失敗時復帰、保持の経過時刻、キャンセル、古い検出結果、連続発火防止、Win32 の透明色設定、venv 子 PID の所有確認を含む。

2026-09-07 16:47 に実機試験を開始。`test_reports/kids_trial_20260907_164753_645218600/` に記録。初回を含む 8 回の表示サイクル時点で COVERED → START → FIRST_FRAME → 旧シーン停止 → REVEALED の順序違反 0、実行エラー 0。指による Next / Back の要求を受信。C922n は実際に名前照合で index 1 が選択され、取得失敗・再接続 0。これはログ上の事実であり、見た目の合格を代用しない。

実機の全面遮蔽・円形進捗・途中キャンセル・Next/Back・タスクバー非表示は Human Check Required。まず「Next に半秒入れて外すとキャンセル、再度約 1 秒保持すると円が 1 周して 1 回だけ幕による切り替え」を質問済み。合格前にコミット／プッシュは行わない。

16:50 の追加観測：実 Manager PID 27412、overlay PID 24748、親ランチャー PID 10528 の稼働を OS で確認。8 回の切り替えで終了記録のある旧シーン・ランチャー計 16 PID を OS と照合し、残留 0。常設 overlay は同じ PID、GDI 20 / USER 13 / handles 660。C922 取得失敗・再接続 0。実機目視結果は引き続き未回答。

16:55 に書体・スタイル更新の読込のため、認証済み operator quit で試験を終了。実稼働 437.281 秒、切り替え 19 回／表示 20 回、exit 0。OS 上でも旧 Manager と overlay の終了を確認した。

追加の表示調整：Bahnschrift の大きい英字、日本語の「1秒キープ」案内、Back の紫／Next のミント色、矢印アイコン、保持時の枠・円・バーを追加。文字とカードは起動時にキャッシュし、判定範囲と保持ロジックは変更していない。幕には不透明面上を流れる曲線を追加。実描画で時刻差による 282,025 ピクセルの変化と、全面遮蔽フレームに透明色がないことを確認した。

表示調整後の Python 3.11 回帰で、既存の認証なし HTTP POST テストに一度 WinError 10053 が発生。該当 10 テストの再実行は成功。失敗ログは `test_reports/navigation_regression_py311_win10053.log` に保存。表示調整との因果関係は確認できていないため、推測による HTTP 修正は行わず全回帰を再確認する。

再確認完了：表示調整後の全回帰は Python 3.11 が 349 件成功（skip 6）、Python 3.12 が 349 件成功（skip 1）。スタイル更新版を `test_reports/navigation_style_20260907.stdout.log` / `.stderr.log` へ記録して再起動。初回表示と Next 切り替えをログで確認し、途中キャンセル・1秒保持・動く幕の全面遮蔽・Back を一括して目視確認依頼中。

ユーザーの「透過」指定により、Next / Back のカード背景と矢印円の塗りを取り除いた。文字・矢印・細い枠のみを映像へ重ね、保持時のリングと枠・バーは維持する。実描画でカードの約 89〜90% が完全透明であることを確認し、保持・遮蔽関連 18 テスト成功。切り替え時の幕は引き続き全面不透明。

生映像をよいタイミングで重ね、シームレスにつながるという追加指定を反映。全面遮蔽が成立した後、同じ共有カメラの左右反転・画面サイズ調整済み映像を約 0.45 秒で最大 96/255 までフェードインし、解除時に約 0.30 秒で引く。幕の開閉には smoothstep を適用し、曲線の時刻は切り替えごとにリセットしない。映像は不透明な幕へ合成してから提示するため、旧／次ウィンドウを透かすことはない。取得から 0.25 秒を超えた映像は表示しない。画像保持は最新一枚と表示用一枚に限定。実描画でカメラ合成と透明色の不存在を確認。タイミング・古い映像の破棄・描画クリップ復元の 4 テストを追加した。

HTTP 切断の切り分けが進展：認証なしの小さい POST を 200 回送ると ConnectionAbortedError が 5 回発生。未読のリクエスト本文を残したまま接続を閉じる経路があった。認証失敗時に本文を最大 2048 バイト・既存 3 秒タイムアウト内で読み切ってから 401 を返す最小修正を行い、同条件 200 回でエラー 0。認証済み操作の条件やカメラ制御には変更なし。繰り返し 100 POST の回帰テストも両環境で成功した。

17:06 の再起動時点：全回帰は両 Python 各 354 件成功（3.11 skip 6 / 3.12 skip 1）。生映像合成と smoothstep を含む最終版は `test_reports/kids_trial_20260907_170644_625049500/` で実機確認中。親ランチャー PID 20320。初回の START / FIRST_FRAME / 昇格を確認済み。Next 途中キャンセル→1秒保持→Back と、生映像を含むつながりの目視確認を依頼中。まだ実機合格・コミット・プッシュはしていない。

子供本人の操作、二人以上の同時操作、12 時間連続運転は今回の短時間切り替え確認とは別の未確認項目として保持する。

## 戻し方

ロゴ・浮遊ボタン・全画面フェード追加後の全回帰：Python 3.11 は 377 件成功（skip 19）、本番 Python 3.12 は 381 件成功（skip 1）。3.12 は numpy / pygame を先に読み込んでから unittest discover し、既存テストの依存スタブではなく実描画のテストを実行。標準 discover では既存 numpy スタブが pygame の import と衝突したため、描画テストはスタブを検知して import 前に skip するよう整理した。旧 Sakura の cleanup は既存の継続処理を維持し、新 TransitionManager のみ失敗時の遮蔽保持を適用した。

ロゴ更新前の実機試験は17:32に operator_quit で正常終了。実表示 1511.329 秒（約25分11秒）、29切り替え／30表示、exit 0。C922取得失敗・再接続0、最大フレーム間隔0.078秒。旧 Manager / overlay の OS 終了を確認してから、更新版を `test_reports/navigation_logo_20260907.stdout.log` に記録して起動。これは新しいロゴ版の30分完走を意味しない。

17:34 の最終読込：文字AAの縁を透明色へ混ぜず影色上へ描く最終調整後、実描画4テスト・compile成功。直前の表示を正常終了し、新版を `test_reports/kids_trial_20260907_173435_212022600/`（stdout: `navigation_logo_final_20260907.stdout.log`）で起動。初回の READY → COVERED → FIRST_FRAME → REVEALED、stderr空、ロゴ無効化警告なしを確認。表示中のコードと最終ファイルが一致する。Next途中退出キャンセル→約1秒保持→Back と、ロゴ・文字・生映像・長いフェード・タスクバー非表示を約40秒で確認依頼中。人間の合格回答はまだなく、コミット／プッシュ未実施。

17:40 の継続観測：同じ Manager PID 29328 / overlay PID 29528 の稼働を確認。実操作による9切り替え、初回を含む10表示サイクルで COVERED → START → FIRST_FRAME → REVEALED の順序違反0、カメラ取得失敗・再接続0。9パターンを選ぶ周期が一巡した。これは操作要求と描画ACKのログ確認であり、9パターンの見え方やキャンセルの人間判定を代替しない。集計は `test_reports/navigation_logo_runtime_review.json`。

17:17 の追加検証：認証済み operator API で select（Fractal 指定）と back を送信し、runtime の scene_request と表示完了を照合。初回を含む 16 サイクルで COVERED → START → FIRST_FRAME → REVEALED の順序違反 0、ERROR 0。HTTP 202 だけを成功根拠にしていない。

例外経路の最終レビューで、overlay 制御接続の切断時に即終了して覆いが外れる経路と、SceneManager.cleanup がシーン停止失敗でも覆いを閉じる経路を発見。前者は通信断後も不透明描画を維持して所有 Job による停止を待ち、後者は全シーンの終了確認まで通常の overlay.close を呼ばず緊急遮蔽を要求する。OS 強制終了そのものまで表示維持を保証するものではない。通常終了と故障注入の回帰で確認する。

ユーザー追加：提供ロゴをトランジションの中核とし、正確な文字列 `KidzDisco Colony` をシンプルで凝った書体で添える。文字にも動きを付け、ロゴ・文字・カメラ映像が連続して現れ消える方針。各シーンの中身には変更しない。

さらに「少ない手数で最大のパターン数」を優先する指定を受け、書体・配色 3 種 × 動き 3 種の組合せを用意する。シーン本体や安全な切替手順は共通のまま、トランジション周期ごとに組合せを選ぶ。同じ切替の途中で別パターンへ飛ばず、画像・文字の読み込みは起動時だけに限定する。

追加の最終方向：左右に閉じる幕より、全画面がゆっくり溶けるフェードを優先。実機表示は全画面の不透明描画を Windows layered window の alpha で 1.2 秒かけて被せ、alpha 255 と描画 flip、DwmFlush の成功後にだけ COVERED を返す。次シーン FIRST_FRAME 後、1.6 秒で解除する。生映像のフェードアウトも 1.6 秒へ延ばす。準備待ちの間は完全遮蔽を維持し、固定時間だけで次シーンを露出しない。idle へ戻る際は透明なボタン画面を flip してから alpha 255 を復元し、一瞬の幕の再表示を避ける。ボタンは透明な面の奥に輪郭・影を重ねた浮遊表現を追加するが、保持判定の領域は固定する。

17:20 の残留照合では、終了記録のある PID のうち現行サンプルの PID を除いた 28 件が OS 上に存在しないことを確認。過去の PID 18804 は現行 Tree ランチャーに再利用されており、PID 数値の一致だけを残留と扱わない。GPU 専用メモリは未取得なので GPU リーク不存在までは断定しない。

今回の試験プロセスを停止してから、既存の `--audience --scene fractal` または `--scene mandala3` の単一シーン起動に戻せる。既存シーンは改変しない。リポジトリの履歴を破壊する reset / force push は不要。

## 次工程の演出要件（2026-09-07 追加）

全シーン共通の方針として、別文書 `SCENE_EXPERIENCE_DIRECTION.md` にも保存した。

**適用範囲の訂正：以下はトランジションや Next / Back への指示ではなく、作品全体を通じた各シーンの中身の演出方針である。** 手全体の色・発光、妖精／ゴーストによるガイド、基本操作の先の隠れたトリガーは、各シーンの次工程に適用する。

**重要な共有イメージ：ユーザーの言葉「その分かりやすさの奥に、隠れた法則が舞ってる」。** 操作の入口と直後の反応は明快にし、遊び続ける中で動きや模様の関係を発見できる奥行きを持たせる。ガイドですべての規則を説明し切らず、試す・気付く楽しみを残す。単純な子供向けゲームに閉じず、大人にも映像作品として成立する質と両立させる。

ユーザー補足：「グーとパーの違いまでは教えても、その先にエフェクトトリガーがある。それは時間でも何でもいい」。基本操作の説明と、その先で発見する追加エフェクトの発動条件を分ける意図。保持時間・動かし方などは候補例であり、発動条件の採用はまだ確定していない。

- 機能の切り替わりを子供が即座に理解できるようにする。例：グーからパーになった瞬間に手全体の色・発光を変える。
- 各機能の操作をゴースト／妖精のアニメーションで実演する視覚ガイドを用意する。
- 時間による自律変化と、子供のアクションで発動した変化を見分けられるようにする。
- この共通演出方針と今回の Next / Back・トランジション実装は別の要件として扱う。各シーン内のグー／パー表現、個別ガイド、隠れた追加トリガーの実装は次工程とし、今回の安定化に混ぜない。
