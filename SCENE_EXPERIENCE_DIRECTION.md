# 各シーンに共通する演出方針

2026-09-07 ユーザー指定。これは作品全体を通じた**各シーンの中身**の方針であり、シーン切り替え UI だけの話ではない。

## 分かりやすい入口と隠れた発動条件

ユーザーの表現：「その分かりやすさの奥に、隠れた法則が舞ってる」。

- グー／パーなど基本操作の違いは伝える。切り替わった瞬間が手全体の色や発光などで明快に分かるようにする。
- ゴースト／妖精のアニメーションなどが、基本操作を視覚的に実演する。
- 基本操作の先には、遊ぶ中で発見する追加エフェクトのトリガーを持たせる。ユーザー補足：「グーとパーの違いまでは教えても、その先にエフェクトトリガーがある。それは時間でも何でもいい」。
- 保持時間や動かし方などは候補例で、発動条件はまだ採用確定していない。すべての条件をガイドで説明し切る必要はない。

## 継続して守ること

- 全体設計として「常に動的に流れている、動き続けている感じ」を重視する。各シーンだけでなくトランジションにも適用し、遮蔽中の起動待ちにも静止した画面に見えない表現を目指す。安全のための不透明な遮蔽は維持し、その表面で動きを作る。
- 全シーンで、時間的な変化によって飽きずに遊べる趣向を持たせる。一気に全体をリセットする表現は避ける。
- 時間による自律変化と、操作をきっかけに発動した反応を、子供が見分けやすくする。
- 子供が動きと映像の因果関係を直感的に理解でき、大人にも映像作品として成立する質を目指す。
- 各シーンの良さを保ち、同じ仕掛けの一律適用にはしない。

本書は共有方針の記録。今回の Next / Back とトランジション管理の安定化を優先し、合格済みシーンへの追加演出の実装は次工程とする。
# Finger Mandala：後続工程の探索（2026-09-07 追加）

追加の確定要望：画面左上の `Finger=…`（指の数の数値表示）は観客向けに不要なので削除する。2026-09-07 の追補作業で、実際の表示元である Tree の HUD のみ削除した。

コード照合：現行 `finger_mandala_acer.py` は `finger_mandala_3.py` を起動しており、その表示ループには Finger 数値の描画は見つからない。一方 `colorfull_tree.py` の `FINGERS = <…>` の生成・描画と専用フォントを削除した。指の数による分岐・片手操作・複数の手の独立描画は維持。既存 Tree テストは Python 3.12 / 3.11 とも12件成功。稼働中 Manager の select 経路で新しい Tree を読み込み、FIRST_FRAME と promotion を確認した（launch_id: 226ba622e86142c287f48392419f6741）。数値が消えた見え方自体の新しい人間判定は未取得。

ユーザー追加：描いた模様そのものも、時間とともに変化していく表現を検討する。モックで見え方を試しながら模索してよく、全体の工程を見て適切な時点で着手する。現在のトランジション作業は中断せず、確認済み Mandala の実装には今は変更を加えない。

既存合意の「一気にリセットしない」「時間による変化と操作のトリガーを区別できる」を維持する。音声の「自然にならないように」の部分は意図が不明瞭なため、具体的な変形方法や自然さの方向は確定しない。モックを表示する段階で見え方を確認する。

## 途中の要望の反映状況（2026-09-07 追補）

- 細長く丸いカプセルの Next / Back、透明感と浮遊感、書体の調整は `c804013` に反映済み。ヒット領域と1秒保持・途中離脱キャンセルを維持している。
- ロゴを中核にした `KidzDisco Colony` の動く文字、書体・配色・動きの組合せ、カメラ映像の重ね合わせ、画面を覆う粒子が落ちて次のシーンが現れる切り替えも同コミットに反映済み。ユーザーは操作・粒子の落ち方・映像のつながりを全て OK と回答。
- 不要な指の数値表示は今回 Tree から削除。実際の指の数を利用した操作処理は残した。
- Mandala の描画済み模様の時間変化は `docs/mocks/mandala-time-evolution.html` に独立した比較モックを作成。固定表示と、回転・微小変形する表示を並べる。急な全消去を行わず、停止・再開も穏やかにする。本番 Mandala / Fractal のコードは変更しない。
- グー／パー切替時の手全体の色・発光、妖精／ゴーストによる基本操作ガイド、その先に隠れた発動条件は今後の各シーン演出として未実装。共有方針として保持し、本番全シーンへの一律適用は行っていない。

残る確認：Tree の数値が消えた目視、Mandala モックの動きの好み。本番 Mandala への時間変化の移植はモック確認後。子供・複数人・最終粒子版の長時間確認は今回の追補テストで代替しない。
