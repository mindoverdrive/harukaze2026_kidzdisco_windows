# SpheresのMVP候補判定と次シーンへの確認順

2026-09-06。基準は候補ブランチの `391ef4f`、稼働中の描画ソースは `233aa50`。元の安全基準点 `705b081` は履歴内に保持する。今回の確認ではアプリのソース、カメラ設定、実行環境を変更していない。

## 現時点の判定

**SpheresをMVP候補として継続し、大人による操作確認の工程へ進める。子供の実地テスト開始と本番採用の合格判定は保留する。** 斜め回転、背景の生映像、指の途切れが減ったことはユーザー確認済み。保存入力の検出と表示座標の自動確認も成功した。一方、実時間の指先と白い円の一致、粒子反応の分かりやすさ、退出・再入場の挙動はまだ人間の承認がない。

次候補の静的確認とpreflightは先に進める。現在の中央確認の回答が来るまで、Spheresの表示を別シーンへ切り替えない。短い操作確認に問題がなければ、Managerの通常終了と資源解放を観測してから、次候補のFinger Colorful Dotsへ進む。この判定を30分・12時間、複数人、子供の操作、安定版への昇格と同一視しない。

## 保存映像と実機の見え方をどう扱うか

| 根拠 | 確認できた範囲 | 確認できない範囲 |
|---|---|---|
| ユーザー「生映像が見えて、指の途切れも減った」 | 背景原輝度表示＋露出-4／zoom 176での実機の改善 | 途切れゼロ、遅延の許容、位置5点一致、各変更単独の効果 |
| 17:28:58の約3秒の保存入力 | 現行検出0.7／追従0.5で保存48/48フレームに手1本。欠落区間0 | 記録時に取得しなかった12回のスキップ、連続した全入力、高速動作や遮蔽 |
| 保存入力のCPU推論 | 中央値31.44ms、最大53.54ms、48フレームの処理約1.97秒 | カメラ露光・USB転送・描画・Xiaomi表示までの体感遅延 |
| 実機のログ | 描画fpsと処理済みカメラ画像の更新fpsを別々に観測 | 描画約50fpsを、手の検出も50fpsと解釈すること |

前の露出-5の暗い入力は手全体を確認できず、後の記録とは手の写り方も異なる。露出だけを変えた因果比較ではない。保存映像で途切れを再現しなかったことを根拠に、現在の検出しきい値は維持する。実機で問題が再度起きた場合は、起きた位置・手の向き・速さ・時刻を確認してから切り分ける。

## 今回の自動確認

- Gitの追跡ファイルに未コミットのアプリ変更はなく、候補ブランチとoriginは一致。別作業のHTML等は変更・追加していない。
- 17:52の読み取りでManager 29272、scene 28660、wrapper 32904が継続していた。APIの実値は露出-4／zoom 176、camera_errorはnull。
- SKYの実表示観測ではSpheresの窓1つ、Manager Controlの窓1つ。球体窓は1920×1080、原点(1920,0)のXiaomi領域にあり、背景映像を表示していた。画面の遅延を測ったものではない。
- 実際のOpenCV画像処理と `display_utils.prepare_camera_frame`、`normalized_to_stage` を使い、1280×720→1920×1080の中央・四隅付近5点を合成画像で確認した。左右反転は1回で、映った点の重心と投影座標の差は最大0.317px（合格条件1px以内）。カメラ・モデル・窓を開かず、実処理部分は約0.078秒。人間によるXiaomi上の5点一致とは別の検証である。
- 次候補の `scripts/start_kids_test.py --audience --scene dots --check` は既存映像Python 3.12.10でexit0。DISPLAY1はprimaryの1920×1080・(0,0)、DISPLAY5はnon-primaryの1920×1080・(1920,0)。依存の更新、物理カメラ取得、シーン起動は行っていない。
- 背景修正後の全回帰199件×2環境は前段階で成功済み。今回はソース変更がないため同じ全回帰を繰り返さず、未確認だった実画像の座標対応と現行構成のpreflightを追加した。

自動確認の根拠は、ローカルの `test_reports/spheres_mvp_geometry_20260906.json` と同名のprobeスクリプト、`test_reports/kids_preflight_20260906_175509.json`。保存映像の根拠と旧終了事象は [Spheres再試行記録](SPHERES_RETRIAL_86590ee_20260906.md) に保持する。

### 継続していた同じプロセスの観測

17:58:27までのログ断面を固定して集計した。scene PID 28660の区間は17:23:41～17:58:27、34分46秒、209サンプル。露出-4適用後に限ると17:28:01～17:58:27、30分25秒、183サンプルで、対象PIDの欠損・JSON破損はなかった。最大サンプル間隔は約10.109秒。これは既に稼働していた映像を観測した結果で、新たな耐久試験を起動したものではない。

露出変更後のscene資源値は次の通り。

| 観測値 | 最初→最後 | 最小～最大 |
|---|---|---|
| Private MiB | 805.39→797.14 | 784.60～805.66 |
| Working set MiB | 260.90→248.02 | 237.91～260.90 |
| Handles | 715→714 | 710～717 |
| GDIオブジェクト | 25→25 | 25 |
| USERオブジェクト | 39→40 | 24～40 |

同じ183サンプルのManager PID 29272も、private_bytesは708,087,808→708,104,192 bytes、handlesは962→872（範囲868～962）だった。この断面ではscene／Managerのメモリ・ハンドルが増え続ける傾向は確認できない。GPU値は両者とも全183件nullで、GPU資源の蓄積は未確認。終了後の残留もまだ測定していない。

描画の集計は中央値50.30fps、範囲46.46～54.20fps。露出変更後の共有カメラ提供は約15.97～16.20fpsで、読み取り失敗・再接続・switch errorは0だった。17:35:12の10秒集計に画像更新11.79fps、検出手数3、スナップショット経過218msがあり、描画は48.66fpsを保っていた。その後画像更新は約16fpsへ戻っている。検出手数は人数ではなく、ライブ推論単体の時間と低下原因は未測定。期間内の検出0も、手が実際に写っていたかと照合していないため検出失敗数とは扱わない。

根拠は `test_reports/spheres_mvp_observation_20260906.json`。資源の集計と描画fpsは10秒前後の周期観測で、短い停止や全フレームの状態を保証しない。正式な30分の総合合格、GPUリークなし、子供・複数人の操作成功とは記録しない。

## 人間に依頼する順序

一度に一手だけ依頼し、回答を確認してから次へ進む。露出・ズームを途中で変更した場合はその値と時刻を別条件として記録する。

| 順番 | 操作と合格の目安 | 現在の状態 |
|---|---|---|
| 1 | 手首まで含めて掌をカメラへ向け、中央で人差し指をゆっくり左右へ動かす。白い円が映った指先にほぼ重なり、その近くの粒の波・発光が分かる | 質問済み、回答待ち |
| 2 | 手全体が映る範囲で左上・右上・左下・右下へ指を順に移し、それぞれ短く止める。白い円の左右逆転や固定的な位置ずれがない | 未依頼 |
| 3 | 手を画面外へ出して戻す。退出中に指の円が残り続けず、球体は動き続け、再入場で再び手の近くが反応する | 未依頼 |

1〜3が通れば、次シーンを比較する工程へ進める。問題があればその具体的な症状をMVPの阻害項目として記録し、直せる根拠が得られた範囲で対応する。明るさや検出設定を推測で連続変更しない。

## 残る確認と運用条件

| 項目 | 扱い |
|---|---|
| 同時に二つ以上の手、二人以上、手の交差・遮蔽 | Human Check Required。max_num_hands=6や並び替えの自動テストは人数6人の実機合格を意味しない |
| 子供が動作と反応を理解できるか | Human Check Required。まず大人の基本操作を確認する。妖精などのグラフィックによる操作ガイドは現行Spheresにない |
| 無人時の演出 | 時間で回転・色・輝度が連続変化し、手なしでも描画を続ける構造は確認済み。長時間の見え方と飽きにくさは目視評価が必要 |
| 単一30分・切替反復 | 起動が継続した時間と、全項目を満たした正式な合格を分ける。異種シーンの交代と退出後の残留も別に確認 |
| 物理USBの抜去・再接続、HDMI切断、12時間 | Human Check Required。今回の表示確認中には実施しない |
| 終了後に再出現したという旧Q／9申告 | 原因未確定。再現操作・推測修正はしない。次シーンへ移る際はローカルUIの「Managerを終了」を使い、終了イベント・子PID・SHM・再出現の有無を別試験として観測 |
| 露出-4／zoom 176の保存 | 現在は未保存の試用値。専用JSONの露出は-5のまま。次起動で-4を比較条件に使う場合は実値の適用・読戻しを記録する。採用値の保存は別の判断 |

## 軽い操作の待ち時間

17:52の認証付きローカル `GET /api/status` は約30.65ms。17:56:35に同じGETを5回だけ連続測定すると、30.666／2.796／1.553／1.635／1.779msで全て成功した。測定範囲では、この読み取りに秒単位の異常な待ち時間は再現していない。設定変更や切替を伴うPOSTは実行していない。

根拠は `test_reports/spheres_mvp_http_timing_20260906.json`。これはHTTP要求から応答までの時間で、ブラウザのクリックから描画まで、Codexのツール呼出し、エージェントの応答待ちの測定ではない。「軽い処理なのに長い待ち」の具体的な操作名と所要時間は未特定のため、原因は断定しない。

現在のscene 28660の起動を同一のruntime elapsed_sで分解すると、spawn→READYは0.329秒、START→ACKは0.093秒、ACK→FIRST_FRAMEは4.032秒、spawn→FIRST_FRAMEは合計4.454秒だった。長い側はACK後だが、その区間内の初期化・処理・描画の内訳は未測定。待受のために一律で睡眠を追加する根拠はない。過去のdots系20交代試験では24起動の合計時間が1.078～1.640秒だったが、シーンと条件が違うため今回の遅延・退行の証明には使わない。

17:23の旧Spheresへの停止要求から出力パイプ終端までは0.234秒。パイプ終端は全プロセス・カメラ・GPUの解放完了時刻ではない。現在のSpheresの終了処理は未観測。新たな切替やQ／9再現はせず、上記の既存ログ集計だけを追加した。

## 次候補と戻し方

次の第一候補は `finger_colorfull_dots_acer.py` → `finger_colorfull_dots_2.py`。希望一覧に含まれ、専用のXiaomi設定と `Start Rebirth Acer.cmd --scene dots` が存在する。元sourceも読み、Spheresと同じ映像・座標の変換、減光しないカメラ背景、手なしの自律波動、共有カメラの所有と解放を確認した。次の短い確認を妨げる新たな不整合は見つかっていない。

Dotsは最大2手・同期推論で、Spheresの最大6手・非同期描画とは異なる。Spheresの描画fpsや手の追従結果をDotsへ流用しない。過去の「反応しない」という申告も解決済みとせず、Spheresと同じ手・画角・露出条件で中央から確認し直す。操作説明・妖精ガイドはDotsでも未実装。

次候補の起動は、現在のSpheresの確認後にManager全体を通常終了し、今回の所有PIDとSHMの解放を確認してから行う。比較結果に問題があれば、次候補のManagerを通常終了して `Start Rebirth Acer.cmd --scene spheres` へ戻す。露出-4は未保存なので、戻す場合も実値を確認する。Gitの強制操作、main/stableへの統合、依存の更新は不要。
