# SaturnとNext/Backカーソルの横ずれ

追加の投影修正後、ユーザーが「ok」と回答。Saturnの最新目視位置合わせを合格として記録。全510テスト、509成功・1スキップ、失敗なし（34.609秒）。

## 追加の固定ずれ（初回修正は目視NG）

ユーザーが「静止しても実映像の指先がカーソルの右上」と報告。初回の比率修正のみでは未解決。
実pygfxの投影行列で検証し、SaturnがFOVを縦画角と解釈していた差を再現。pygfxは平均幅高さの画角で、16:9では縦方向へ2/(1+16/9)=0.72を掛ける必要がある。同一指座標が期待(192,108)に対し(-106.67,-60)へ投影されていた。
背景平面の高さと_landmark_to_worldのhalf_heightへ同じ係数を追加。実投影テストは修正前失敗、修正後四隅と中央で0.01px以内。背景と検出点を一緒に正すため物理演算の係数変更やOverlay座標のオフセットは加えない。最新目視は再確認待ち。

- ユーザー報告：Saturn内の検出点と生映像は一致するが、操作カーソルがずれる。
- 原因の再現：Saturn初期化だけがCameraResizingProxyに640×480を指定。1280×720の共有映像を4:3へ変形した後、16:9画面へ余白付きで配置する。Overlayは元の16:9入力で配置する。同一正規化座標(.05,.1)がSaturn(312,108)、Overlay(96,108)となる。
- tests/test_saturn_navigation_coordinates.pyで実際の初期化中のcap.set呼び出しをProxyへ適用し、同じ指座標の差を再現（修正前失敗、修正後成功）。
- 最小修正：Saturnの強制640×480指定を削除。camera_layoutは最初の実フレームから取得する。物理カメラ設定、Overlay、ホールド判定、Saturn物理演算に変更なし。
- 映像も指検出も元のアスペクト比へ揃うため、従来の横圧縮・左右余白は解消する。これはカーソルだけを任意にずらす補正ではない。
- 人間による左右ボタン付近の位置確認待ち。未コミット。
- 最終全体回帰509件、508成功・1スキップ、失敗なし（33.502秒）。test_reports/saturn_navigation_tests.log。
