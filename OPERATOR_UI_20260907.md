# 最小オペレーターUI

## 最終確認（2026-09-08）

- ユーザーが運営用UIの実機確認に「すべてok」と回答。直接指定、Next/Back、Restart、Safe/解除、カメラ調整の操作確認として記録。以下の回答待ち記述は経過記録。
- 最終コード起動でWindows Job準備経路にWinError 5のアクセス拒否が1回発生。対象プロセス終了後、設定変更なしの再試行でFractalのFIRST_FRAME（2.703秒）を確認。恒久的に解消したとは扱わず、起動失敗の再現調査を残す。証拠: operator_ui_final.stdout.log / operator_ui_final_retry.stdout.log。
- 実機操作で保存された露出-3・ズーム333を運営用プロファイルに維持。現場の推奨値とは扱わない。
- 自動テスト最終結果: Python3.12は465成功・26スキップ、Python3.11は412成功・72スキップ。専用透過率7件成功。
- 既存シーンのソースとトランジション描画は変更なし。表示用カメラフレームの共通経路のみ、濃さ指定時に減光する。
- 残課題: 起動時アクセス拒否の再現条件、子供・複数人・12時間試験。目視合格をこれらの合格へ読み替えない。

基準: c4779b7。候補ブランチcodex/rebirth2026-production-candidate。実機判定が終わるまでコミットしない。

## 実装

- operator_panel.htmlの同一ページに12シーン一覧、現在・次、Back/Next/Restart、Safeと解除を追加。
- 全操作はManagerの要求経路を使用。Restartも現在のシーンの直接指定として、FIRST_FRAME後に旧シーンを終了する。
- Safeは既存全面演出を保持する。黒画面ではなく、ロゴとカメラを含む。切替中の要求も保持し、解除中なら解除完了後に再遮蔽。確認済み遮蔽と遮蔽待ちを分けて表示する。
- Safeは通常の未処理操作より優先。解除は生存するシーンと遮蔽完了が条件。Restart/直接選択で遮蔽から新シーンへ進める。
- 露出・ズームの既存適用/実値表示/JSON保存を維持。
- 映像の濃さ0–100%は表示用stage_frameのみ減光する。検出用camera_frameは変更しない。100%は同一画素・同一オブジェクトの無変更経路。Manager寿命内の一時ファイルで次シーンへ引き継ぎ、再起動時は100%。
- 元プロファイルを維持し、configs/rebirth_operator_acer_xiaomi.jsonに全12個のAcerラッパーを明示。元シーンおよびトランジション描画は変更しない。

## 起動

実行環境のPythonで `scripts/start_operator.py` を実行。物理カメラはManagerのみが所有する。起動ログの認証付きURLをAcerで開く。Xiaomiは観客用、Acerは操作用。

## 検証経過

- Python3.12 全488件中463成功・25スキップ（operator_full312_pass.log）。透過率専用は別実行6件成功。
- Python3.11 全481件中410成功・71スキップ（operator_full311.log）。
- その後Safeの失敗候補・解除途中の2境界テストを追加、操作経路専用7件成功。製品コード変更なし。
- 実ブラウザのUI読取で12シーン、現在/次、操作ボタン、露出-5、ズーム176、濃さ100%の表示と接続中を確認。
- 実機の最初の直接指定目視判定は回答待ち。FIRST_FRAMEだけで目視合格としない。

## 残り

最終レビュー更新: Safe中の自動切替タイマー・clapによる解除を抑止。3.12は491件中465成功・26スキップ、3.11は484件中412成功・72スキップ。operator_review_final312.log / operator_review_final311.log。表示用フレーム統合テストも追加し、濃さ0でも検出入力と座標layoutが変わらないことを確認（専用7件成功）。現在の実機プロセスはタイマー抑止追記前に起動しているため、最終確認時に新コードへ再起動が必要。現試験では自動タイマー未使用。

直接指定、Next、Back、Restart、Safe/解除と同期、透過率変更と切替後維持、露出/ズーム操作の人間確認。結果を追記後、差分を1コミットへまとめる。子供・複数人・12時間確認は別項目のまま。

ロールバックはc4779b7の製品コードと元起動手順。今回の運営用プロファイルを使わなければ既存プロファイルはそのまま利用できる。
