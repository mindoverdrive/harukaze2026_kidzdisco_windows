# Next / Back とトランジションの採用記録

## 採用確定

ユーザーが最新の実機表示に「これを採用」と回答。フラットカプセル、大きい方向矢印、白線なし、元の色のタッチ発光の上を虹色の外周ゲージが一周する見た目を採用する。ぼかしは完全破棄。確認時の起動ログはtest_reports/navigation_glow_rainbow.stdout.log。

最新指示: ぼかしの方向性は完全破棄。ぼかし機能は追加しない。タッチ中の元のアクセント色の輪郭発光を戻し、その上に虹色の外周進捗を重ねる。白線2本は削除したまま。

基準92ef91c。後続指示により、当初維持予定の指周囲の円形ゲージをカプセル外周ゲージへ変更し、白い反射線2本も削除した。

- 背面のオフセット輪郭・接続線・厚みの影を除去。
- カプセル形状・文字・検知領域は維持。矢印は円枠を外し、方向線を太く大きくした。
- ゲージは上中央から時計回りに外周を一周。時間で色相が変わる虹色と暗い下地線で背景との分離を助ける。完全な全背景での可読性保証ではない。
- NavigationHold、1秒判定、退出キャンセル、発動後ラッチは変更なし。Manager/TransitionManager/シーン実装は今回変更しない。
- 描画・判定テスト26件成功。実描画プレビュー navigation_rainbow_preview.png を確認。
- ぼかしの調査は終了。ユーザー指示により実装せず、採用対象から除外。

## トランジション

- オーバーレイ側のカメラ背景表示を停止。カメラ映像は次シーンの一部として、崩れたブロックの隙間から現れる。
- 既存の不透明な断片描画と全面遮蔽を維持。画面外に出るまでブロック自体をフェードさせない。
- 全面遮蔽中の装飾ブロックは、それぞれの位相と複数の周期を組み合わせて動く。遮蔽の土台は不透明なまま。
- 最新ソースを再起動反映。FIRST_FRAME 2.734秒。起動ログ: test_reports/transition_no_camera.stdout.log。
- Nextを約1秒保持し、カメラ背景なし・ブロック不透明・隙間から次シーンが現れる確認に対し、ユーザーが「ok」と回答。今回の実機目視確認は合格。
- 関連トランジションテスト70件成功。長時間耐久、子供確認、実際の複数人操作を今回の目視確認で代替したとは扱わない。

## 基準と戻し方

コミット前の全体回帰: Python 3.12で493件実行、467件成功・26件スキップ、失敗なし（21.533秒）。ログ: test_reports/navigation_transition_commit_tests.log。描画の関連テストは上記の独立実行結果も参照。

基準は候補ブランチの92ef91c。mainは変更しない。必要時はこの変更コミットをrevertして戻す。既存シーン・Manager・TransitionManagerの制御ロジックは変更していない。
