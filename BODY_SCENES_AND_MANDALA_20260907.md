# 身体シーンとMandala色変化の実装・確認

## 最新の確認結果（以下の経過記録より優先）

2026-09-07、基準 `69c628a` からの本番候補更新。人間が個別に判定できる状態まで実装・自動検証し、次の基本目視結果を得た。

- Sci-Fi Jacket: 静止判定の修正後「だいたいOK」。追加した3モチーフ（波・輪・編み目）の絡み方に「ok」。服色・身体内の流れ・余韻・静止時結晶化/解除を実装。
- Skeleton Glitch: 基本目視「ok」。黒背景・生映像なしをユーザーが明示確定。単純な身体線を維持し、グリッチ色の時間変化と薄い残像を追加。
- Finger Mandala 3: 色変化に加え、ゆっくり外へ広がって薄れる追加仕様を実装。30～60秒の確認に「ok」。カメラ/カーソルには変形や色変更を適用しない。
- Re:birthトランジション: 原本JPEGはSHA256一致で保存。白背景と内部の白領域のみ描画時に透明化し、ロゴと元の文字を一組にしてColonyとcycleごとに交互表示。白抜き表示の目視に「ok」。生成した加工案は不採用・製品未参照。
- 既存Fractal/Storm/Spheres/Treeのコード、manager、scene runner、共有カメラ、SceneManager/TransitionManagerの遮蔽と安全切替制御はこの更新で変更しない。

最新全体テスト: Python3.12で475件中451成功・24skip、3.11で468件中398成功・70skip、失敗なし。ログ `test_reports/rebirth_cutout_full312.log` / `rebirth_cutout_full311.log`。mockの影響で全体実行ではskipされる実描画/実配列は専用プロセスでも検証（Jacket14件、Mandala色5件・拡散4件、Skeleton26件、ロゴ白抜き4件）。これらは同じテストの再実行を含み、件数を加算して総成功数としない。

### 残すHuman Check Required

1. 最終コードでの各シーン30分連続完走。今回の30分設定試験は目視後に次シーンへ正常切替しており、設定しただけで完走とはしない。
2. 実際の複数人による接近/交差、Jacketの人体mask結合、Skeletonの人物切替、Mandalaの人ごとの色と操作。
3. 子供の操作・現場照明・Xiaomiでの視認性。成人一人の目視とは別項目。
4. 全ブロック配色/形状/解除動作の組合せと長時間切替。ロゴ白抜きOKだけから全パターンを合格にしない。
5. 12時間耐久は未実施。

新Jacket/Skeletonは単独試験入口に追加済み。本番navigation playlistへの追加・本番昇格は行っていない。既存navigationで動作するMandalaとトランジションは新版を使用する。

起動: 映像用Pythonで `scripts/start_kids_test.py --audience --scene jacket --duration-minutes 30`、`skeleton`、`mandala3`。切替試験は `--audience --navigation --duration-minutes 30`。同時起動せず既存ManagerをUIの終了経路で止めてから起動する。

ロールバック基準は `69c628a`。候補ブランチ上の本更新コミットをrevertするか、別worktreeで基準を確認する。main/stableのreset・強制pushや無関係な作業の一括破棄はしない。

以下は途中の観測・判断経過。各節の「未確認」「未コミット」はその時点の状態であり、最新状態は上記を参照。

## 合意と保護範囲

2026-09-07の新ゴール。順番はSci-Fi Jacket、Skeleton Glitch、Mandalaの描画色。
基準コミット `69c628a` と、着手前から未コミットだったブロックトランジション追加版を維持する。
既存Fractal・Storm・Spheres・Tree等の挙動と、SceneManager／TransitionManagerの安全な切替経路は変更しない。

- Jacket：服付近の色を使った身体内の流れ、動いた後の余韻、静止保持で徐々に結晶化し動作でほどける。検出枠・識別番号の表示は撤去。
- Skeleton：既存のシンプルな灰色の身体線と白い関節、黒背景を維持。動作グリッチの色だけ時間で移ろわせ、少数の薄い過去姿勢を追加。残像の採否は人間の見た目判断を残す。
- Mandala：新規描線だけでなく描画済みの色も変化。原画の形・alpha・人ごとの色差を保持。生映像には色変換をかけず、急な全消去をしない。

## Mandalaの方式

`mandala_colors.py` の表示用Surfaceだけ色相を120秒で一巡させる。元のcanvasは変更せず、毎回そこから表示色を計算するため色変換の誤差を累積しない。色相段階が変わらない間は全面変換をせず、新しい線だけ原画と表示面へ即座に描く。表示用の大きな配列とSurfaceは再利用する。

初期検証：既存Mandala27テスト、新規色変化5テスト成功。原画・透明度保持、人ごとの色差、周期終端の原画復元、キャッシュ中の即時描線、Surface/配列再利用を確認。実機の見え方と負荷は未確認。

1920×1080の純Surface再検証：alpha 0/1/127/128/254/255保持、原画完全復元、ロック残留なし。表示用固定メモリは約25.7MiB追加。全面色更新は約0.469秒ごと、キャッシュ命中は約0.0018ms。更新時は22.45〜24.85ms（初回32.54ms）を観測し、実機の瞬間的なカクつきを確認項目とする。行優先配列への調整による速度改善は確認できていない。旧計測と並行Fractal負荷が異なるため差の原因は断定しない。記録：`test_reports/review_mandala_colors_cpu.json`。

## 実機起動手順

映像用Pythonで `scripts/start_kids_test.py --audience --scene jacket --duration-minutes 30`。
次は `--scene skeleton`、最後は `--scene mandala3`。入口は一時的な単一シーンprofileを生成し、保存済み本番playlistやカメラ設定を上書きしない。新規body detectorの存在はpreflightで検査する。

一度に一つだけ起動し、前のManagerを認証済みquitで終了・プロセスとカメラ所有権の解放を確認してから次を起動する。
子供、実際の複数人、12時間耐久は成人1人の基本目視や30分試験とは別に残す。

## 編集中の保存と実行の競合

継続中だった `kids_trial_20260907_182134_759127400` が、Mandalaの色クラス参照を追加した直後・importを追加する前の中間状態を自動選択し、`NameError: ArtworkHueCycle` で候補破棄→試験終了した。起動失敗を合格とは扱わない。
その後import追加と関連テスト、実ファイルのmodule importを確認済み。旧Manager/overlayのプロセス不在を確認した。以後は編集中のファイルを自動選択させず、未変更Fractalの単一表示へ戻してから、完成した版を個別に検証する。

## 未確認

外向き拡散＋色変化のMandalaに対する30～60秒の目視依頼へユーザーが「ok」と回答。外へ広がる速さ・薄れ方・色変化・追加描画の基本目視を確認済みと記録する。30分耐久・子供・複数人の合格とは区別。次にRe:birth原本とColonyのトランジション交互表示を確認する。

## 追加仕様: Mandalaの外向き拡散

ユーザーから、描画済み線を「ゆっくーりと外側に向かって広がって消えていく」よう色変化と同時に実装する指示。この時点から、従前の「形を固定する」は更新され、元canvasと色cacheを同じ変形・減衰で動かす。カメラとライブ指カーソルは対象外。新しい線は従来通り指位置に即座に描画する。

初期方式は1.2%/秒の中心からの拡大、alpha2/秒の減衰を8Hzで適用。専用実配列4試験で外向き重心移動、消滅、新規線の即時性、色cacheとのalpha同期、色更新で古い線が復活しないことを確認。1080p初期実装は約52ms/更新と重いため、この中間版は実機へ反映せず軽量化中。Mandalaは旧色変化版の単独表示を継続。

軽量化後: Surfaceのnative packedメモリを直接変形し、固定RGBA出力1枚のみ使用。独立A/B検証で以前版と8stepのRGBA差分0（低alphaと減衰を含む）、lock残留なし。2面更新中央値7.18ms、5.69～17.34ms。固定バッファ約7.9MiB。証拠: `test_reports/bench_mandala_packed.json`。現行実装はこの軽量化版。処理の軽量化は確認済み、実画面の滑らかさは人間確認に残す。

全体回帰はPython3.12で474件/24スキップ、3.11で467件/69スキップ、失敗なし。実配列4試験は別プロセスで成功。旧Mandala正常終了・プロセス不在確認後、`mandala_outward_20260907.stdout.log`へ新しい単一30分試験を起動。外へ広がる速さ・消える速さ・色変化・追加描画の実機目視は未確認。Re:birthの交互表示実機確認はその後へ。コミット・pushは未実施。

Mandala色変化の単独試験を起動。Skeletonをquitして既知プロセスの不在確認後、`--scene mandala3 --audience --duration-minutes 30`。FIRST_FRAME=1.063秒、scene PID11924。ログ: `test_reports/mandala_colors_goal_20260907.stdout.log`。描いた線の保持・色の自然さ・カメラ色不変・追加描画時の引っかかりを目視待ち。

Skeletonの初回目視依頼にユーザーが「ok.skeltonは、黒画面で。カメラ映像は無し。」と回答。身体の線・動作/残像の基本目視を確認済みとする。黒背景・生カメラ映像を表示しない仕様を明示的に確定。検出のためのカメラ入力は使うが、観客映像には合成しない。現実装は既にこの仕様のため追加コード変更なし。複数人・長時間試験は別途未確認。次はMandalaの描画済み線の色変化を目視する。

Skeleton単独目視を開始。Jacketを正常終了し既知プロセス不在を確認後、`--scene skeleton --audience --duration-minutes 30`で起動。FIRST_FRAME=1.344秒、scene PID18932。ログ: `test_reports/skeleton_goal_20260907.stdout.log`。この通知は表示処理の開始確認であり、人の検出・線の見え方・残像の採否は未確認。最初は一人で確認し、複数人は別項目として残す。

## Jacketモチーフ追加

新モチーフ版の目視依頼にユーザーが「ok」と回答。複数模様の絡み方の基本目視を確認済みとして記録する。30分完走・複数人・子供の操作確認をこの回答から合格にしない。次はSkeleton Glitchの単独目視へ進む。

全体回帰: 3.12で467件/20スキップ、3.11で460件/62スキップ、失敗なし。実配列14件は別プロセスで成功。前試験を正常終了し既知プロセス不在を確認後、新モチーフ版を単一30分設定で起動した。出力: `test_reports/jacket_motifs_20260907.stdout.log`。見え方と30分完走は未確認、commit/pushなし。

ユーザーは静止修正後に「だいたいOK」と回答。静止/解除は概ね確認されたが、全演出の最終合格とはしない。追加要望: 単調なうねる線を、3種類以上のモチーフが絡み合う終わりのない流れへ。

`jacket_effect.py`内の流れのみ更新。波のリボン、移動する輪、菱形の編み目を追加し、それぞれの場が他の模様の座標・位相を変調する。全3種を残しながら異周期で重なり比率を変え、一括切替/消去を行わない。連続検出中は位相を剰余で巻き戻さず連続進行。結晶化時にも15%の流れを残す。退出や長い入力断では従来通り状態を破棄するため、その再入場まで「一切リセットなし」とはしない。

専用実配列14テスト成功。静止・解除・ノイズ耐性・原画/マスク外保持に加え、3場の相違・連続性・12時間相当時刻での有限値を確認（12時間の実耐久試験ではない）。静止画モック `test_reports/jacket_motifs_preview.png` も確認。独立レビューのrender中央値13.394ms（前9.779ms）、1080p合成12.79～13.70ms、mask外変更0px。推論込み実FPS・モチーフの見分けやすさは実機確認に残す。

## Jacket静止結晶化NGへの修正

ユーザーから「静止で結晶化していない」と報告。初回表示は結晶化について不合格であり、30分経過してもこの演出の合格にはしない。

再現: 静止画像に対し人体maskのみ左右±1px揺らすfixtureでは、8秒後もcrystal=0。`test_reports/jacket_stillness_before.log`。重心の毎フレーム差分を時間で割った速度が静止閾値を超え、quietを毎回リセットすることを確認。実入力でこれが唯一の原因かは未確定。

最小変更: `jacket_effect.py`で静止判定用の重心のみ時定数0.30秒で平滑化。可視模様の位置・余韻anchor、服色、合成、カメラ設定は変更しない。位置揺れでも結晶化する回帰試験が修正前失敗→修正後成功。内部動作・輪郭形状変化による解除試験も成功。実機目視で再確認する。

追加確認: 実NumPy/OpenCVで専用12件成功（微小カメラノイズ、継続移動での解除を含む）。全体3.12は465件/18スキップ、3.11は458件/60スキップで失敗なし。全体試験ではmockの影響でこの実配列fixtureがスキップされるため、専用試験を別プロセスで実施した。旧Jacketをquitし既知プロセスの不在確認後、`jacket_stillness_retry_20260907.stdout.log`へ30分設定で再起動。結晶化の再目視はHuman Check Required。まだcommit/pushなし。

## 個別実装の自動検証（実機目視前）

- 実行用Python 3.12で `skeleton_glitch` / `finger_mandala_3` の実モジュールimport、Pose/FaceDetection API、ArtworkHueCycle参照を確認。カメラ・追加ウィンドウを開かず成功。Jacket PID 9988は継続稼働を再確認。単発のメモリ・ハンドル観測だけでは長時間リーク合否を判定しない。

- Jacket: 服色を基に身体内の流れ・余韻を描画し、静止2秒後から約3秒で結晶化。動作で解除。検出枠・IDを撤去し、共通カメラ配置を使用。専用17テスト成功。
- Jacketの1080p合成は全面NumPy float演算からOpenCV合成へ変更。同じ合成fixtureで105～116msから11.27～13.70ms。640×360演出生成の中央値9.78ms。推論込みの実FPSは未確認。拡大マスク外の変色は2398pxから0px、原画とletterboxの保持を確認。証拠: `test_reports/review_jacket_cpu.json`。
- Skeleton: 灰色2pxの骨格・白点・黒背景を維持。グリッチ色を90秒周期で変化させ、薄い過去姿勢を各2枚まで表示。最大3人のPoseと履歴を分離。Python 3.12専用26テスト成功、3.11は23成功・描画3スキップ。静止描画は時刻を変えても実画素一致。
- 共通起動スクリプトに単一試験用 `jacket` / `skeleton` を追加。保存済み本番playlistは変更していない。
- 全体テスト初回は起動チェックmockの引数不整合が2件。新しいscene引数をmockでも受け取るよう更新して再検証する。
- ユーザーが現在の表示を「Fractal Finger」と呼んでいることを確認。実際の起動対象は `fractal_moving_acer.py`。今回の新演出が表示済みとは扱わない。
- 再検証: Python 3.12は462件中447成功・15スキップ、3.11は455件中398成功・57スキップ、両方失敗なし。証拠: `test_reports/body_goal_full312.log` / `body_goal_full311.log`。
- 19:04: Fractalを認証済みquitで終了し、既知4プロセスの不在を確認。Jacket単一30分試験を起動。C922nの物理取得1280×720/30fps診断を通過、FIRST_FRAMEは1.125秒。これはカメラ診断FPSでありJacket描画FPSとは異なる。試験: `test_reports/kids_trial_20260907_190413_887208500`。実際の見え方・演出と30分完走は未確認。コミット・pushはまだ行っていない。

全3件の新演出はHuman Check Required。まだ本番playlistへ追加せず、完成・合格やコミット済みとは記録しない。
